#### Group C

| Axis | Score | Evidence | What would raise it by one |
|---|---|---|---|
| Completeness | 3 | Round-2 gaps are closed: `06-services/bff-web.md:120` adds "`GET /bff/web/v1/internal/ai/sources/{sourceEntity}?cursor=` \| **Internal**: Ai's client-credentials service token". `platform.md:670`, `699` and `724` embed the saga diagrams, and every sheet now opens at "## 1.". New gaps. (1) Doc 11 still does not route five services' render commands. `11-messaging-architecture.md:490` binds `documents.commands` "also on `nibras.admissions`, `nibras.school`, `nibras.assessment`, `nibras.finance`, `nibras.requests`". Yet Hr, Behavior, Operations, Scheduling and Wellbeing send `GenerateDocument`. `hr.md:1440` says "Hr could not render letters and payslips". (2) `34-work-breakdown.md:1193` (SL-OPS-624) builds a "Digital twin composer" in Bff.Web, but "twin" appears 0 times in `bff-web.md`. (3) The Academics sheet specifies no aggregate, route or folder for REQ-ACA-028. Its slice at `34-work-breakdown.md:1040` depends on "the Academics daily sheet OpenAPI from phase 2" | Bind `nibras.behavior`, `nibras.hr`, `nibras.operations` and `nibras.scheduling` on `documents.commands`, and `documents.document.generated.v1` on `hr.events`, in doc 11 §2.4 and §2.5. Add the digital-twin composer route to `bff-web.md` §4.1, with its permission, upstreams, budget and tree entry. Specify the daily-sheet aggregate in the Academics sheet, and leave only the permission pending |
| Consistency | 3 | The round-2 contradictions are fixed. `10-data-architecture.md:319`, `11-messaging-architecture.md:575` and `ai.md:355` now all bind the six keys on `ai.index-sources.bulk`. The stale open points are closed (`identity.md:2376`, `finance.md:1711`). Remaining drift misleads a builder. `hr.md:1445` says "Hr runs no model" and "The rung 2 workload model needs a Reporting owner". But SL-HR-616 (`34-work-breakdown.md:1131`) puts "a rung 2 strain suggestion" in Hr. `operations.md` open point 10 says occupancy is shown only "unless Bff.Web also reads Attendance", while SL-OPS-624 reads "Attendance roll-call counts". Two sheets give different defaults for one open decision. `wellbeing.md:67` says "Documents renders only what Wellbeing sends in a `GenerateDocument`". `documents.md:1378` says "Nothing Wellbeing renders here". `11-messaging-architecture.md:1061` calls TC-MSG-901 "the screen's UI test ... in `08-web-structure.md`". The registry defines it in the Platform sheet as an integration test | Make the Hr sheet and SL-HR-616 agree on who owns the rung 2 workload model, and name that owner in the Reporting or Hr sheet. Make Operations open point 10 match SL-OPS-624. Give Wellbeing open point 11 the same interim default as Documents open point 13. Correct doc 11:1061 |
| Feasibility | 4 | The round-2 raise is done. `34-work-breakdown.md:102` (SL-MSG-001) says the dispatcher spike is "scheduled here, in phase 1 rather than phase 6", read "against the 5-second line". Unknowns carry owners and defaults, e.g. `11-messaging-architecture.md:1134` names the lease split as the fallback. Residue: until doc 11 binds the render commands, payslips, contracts, purchase orders and award certificates cannot be produced as written. Doc 11's open point 5 still says "the spike is for document 34 to schedule", which is stale | Close the render-command bindings (see Completeness). Update doc 11 open point 5 to cite SL-MSG-001 |
| Risk honesty | 4 | Docs 10, 11 and 21 now state why unregistered rows stay local. `11-messaging-architecture.md:1074` says "none; score 10, local", and the header says the owner re-scores at each phase gate. Sheet risk tables use doc 18's scale, e.g. `wellbeing.md:1032` "\| 4 \| 5 \| 20 \|" linked to RISK-47. Some points are understated. `academics.md:968` scores a signature feature's missing endpoint at 2 x 2 = 4 and says "No open question owns this". `hr.md:1440` gives likelihood 2 to an outcome that is certain until doc 11 changes | Re-score Academics open point 1, which is the gated demo of feature 19, and Hr open point 2, which is certain until doc 11 is fixed. Record the Appendix B daily-sheet permission as an Open Question |
| Testability | 4 | `11-messaging-architecture.md:1048` says "Each suite carries one test-case identifier from this document's block `TC-MSG-750` to `TC-MSG-759`". `10-data-architecture.md:679` says "`TC-DATA-001` to `TC-DATA-006` are the WF-DATA-01 import transitions ... not cited here for any other meaning". `attendance.md:1146` says "**The timing clause is conditional on Open Question 27**". Residue: doc 11:1061 gives the wrong location and kind for TC-MSG-901 | Correct the TC-MSG-901 citation. Give Identity's right-to-left row a test id in place of "Notification's template tests" |
| Distinctiveness | 4 | Every sheet and docs 10, 11 and 21 now carry a signature-features line that points to one trace. `32-product-differentiation-and-demo.md:86` says "This is the one table that traces the 43 features end to end". `21-performance-engineering.md:1266` ties budgets to features 1, 3, 6, 32, 34 and 44. Three owned features still cannot be built from their sheets. Feature 43's rung 2 model has no owner (`hr.md:1445`). Feature 33's composer is missing from Bff.Web. Feature 19 has no Academics specification (`academics.md:22`: "has no endpoint here until Appendix B names its permission") | Name an owner and a sheet section for feature 43's rung 2 model. Specify feature 33's composer in Bff.Web and feature 19's aggregate in Academics |
| Portability | 4 | Each sheet has a platform-notes table naming the runner, e.g. `wellbeing.md:1013` "Right to left ... `TC-L10N-701` (document 08)" and `10-data-architecture.md:590` for `ar-x-icu` collation with "`TC-DATA-750` on `ubuntu-latest`". Weak proofs: `notification.md:855` and `wellbeing.md:1014` cite `TC-PLAT-009`, the font-shaping device check, as the no-Google proof. `identity.md:2298` offers "Notification's template tests" with no id | Cite the no-Google device-pass test of doc 33 §171, not TC-PLAT-009. Give Identity's right-to-left row a test id |

**Verdict:** Blocked (blocking axes: Completeness, Consistency)

| Round-2 gap | Now | Evidence |
|---|---|---|
| Ai index named `embeddings`/`scope_code` (ai.md:491 residue) | Closed | `ai.md:1012` open point 7: "documents 10 §5 and 21 §1.20 and §3.20 now carry that name and the `ix_chunk_*` indexes"; no `embeddings` table remains in the sheet |
| No link from mechanisms to signature features | Closed | 23 of 23 sheets and docs 10, 11 and 21 carry "**Signature features.**" lines, e.g. `11-messaging-architecture.md` §14, all pointing to the `32-product-differentiation-and-demo.md:86` trace |
| Sheets never name runner, OS, RTL or no-Google behaviour | Closed | Every sheet has Platform notes with a runner column, e.g. `notification.md:854` "Right to left ... TC-NOT-604, TC-NOT-618 \| `ubuntu-latest`". Some proofs are weak (new gap) |
| About 34 brief-findings open; workarounds pending ADRs | Partly closed | 26 "Still open" rows remain, each scored and owned. The ones only doc 11 can fix are still open: `hr.md:1440`, `behavior.md:923`, `scheduling.md:1050` and Documents open point 7 |
| Saga orchestrators and other workflow owners lack state diagrams | Closed | `platform.md:670`, `699`, `724`; `school.md:625`, `655`; `admissions.md:424`. The five sheets with none own no workflow, e.g. `ai.md:367` "owns no workflow" |
| Doc 11 suites lack TC ids; TC-ATT-003 30 s unconditional | Closed | `11-messaging-architecture.md:1048` gives the block TC-MSG-750 to 759. `attendance.md:1146` makes the timing clause conditional on Open Question 27 |
| Ai source-change indexing contradiction (ai.md vs doc 11 §2.5, doc 10:319) | Closed | `11-messaging-architecture.md:575` binds six keys on `ai.index-sources.bulk`. `10-data-architecture.md:319` says "all six bind on `ai.index-sources.bulk`". Doc 11 open point 7 is closed at line 1136 |
| Bff.Web omits the internal Ai routes; natural-language search is synchronous | Closed | `bff-web.md:120` to `122` add the three routes. `bff-web.md:117` answers "**202** `{ jobId, location }`". TC-BFF-750 to TC-BFF-753 and `gateway.md:97` cover them |
| TC-DATA-001..003 cited as pooling tests; wrong TC-DATA-006 range | Closed | `21-performance-engineering.md:1033` and `10-data-architecture.md:665` cite TC-DATA-643 to 645. Line 581 cites TC-DATA-642. Line 679 fences TC-DATA-001 to 006 |
| Doc 11 guarantees had class names but no TC ids | Closed | `11-messaging-architecture.md:1050` to `1066`: one TC-MSG-75x per suite, cited again in "How this document is verified" |
| Stale open points against the fixed doc 21 (identity 9, gateway 1, finance 9, ai 5) | Closed | `identity.md:2376` "Closed 2026-09-26"; `gateway.md:433` "Closed"; `finance.md:1711` "Closed 2026-09-26"; `ai.md` open point 5 "Closed 2026-09-26" |
| No sheet or architecture document ties a mechanism to a feature, moment or demo test | Closed | `10-data-architecture.md:595` and `21-performance-engineering.md:1266` name the features served and defer the moment, rung and demo test to the doc 32 trace |
| No runner, OS, RTL or no-Google coverage in 22 sheets and docs 10, 21 | Closed | `10-data-architecture.md` §12 and `21-performance-engineering.md` §14 add runner, culture, time-zone, RTL and no-Google rows with test ids |
| 14 sheets without a state diagram; four sheets start at section 2 | Closed | A grep of "## 1." finds all 23 sheets opening at section 1. Diagrams as in the row above |
| Seven risk rows never entered in doc 18; sheet risk tables use low/high | Closed | `11-messaging-architecture.md:1070` says a row with no register id "scores below 12 ... and stays local". The sheet risk tables carry numeric L, I and Score (`wellbeing.md:1030`) |
| TC-ATT-003's "within 30 s" not conditional on Open Question 27 | Closed | `attendance.md:1146` says "**The timing clause is conditional on Open Question 27**, which is still open" |

| Gap | Axis | Effort to close | Blocking |
|---|---|---|---|
| Doc 11 §2.4 and §2.5 do not route `GenerateDocument` from `nibras.behavior`, `nibras.hr`, `nibras.operations` or `nibras.scheduling`, and do not bind `documents.document.generated.v1` on `hr.events`. As written, payslips, contracts, purchase orders, award certificates (feature 18's family export) and timetable PDFs cannot be produced. Each sheet records this as an open point owned by the document 11 owner, a plan-side fix | Completeness | 2 hours (plus the Appendix E consumer entries for the generated event) | yes |
| Bff.Web has no digital-twin composer, although SL-OPS-624 builds one there and Operations says Bff.Web composes feature 33. Operations open point 10 says Attendance is not read; the slice reads Attendance roll-call counts | Completeness | 2 hours | yes |
| Feature 43: `hr.md:1445` says Hr runs no model and the rung 2 model needs a Reporting owner. SL-HR-616 builds the rung 2 strain suggestion in Hr, and the Reporting sheet never mentions workload | Consistency | 2 hours plus an architect decision | yes |
| For one open decision (RISK-48), Wellbeing open point 11's default has Documents render referral packs, and Documents open point 13's default refuses them. Wellbeing's `DocumentRequestPublisher` would publish into an unbound queue | Consistency | 30 minutes (align the interim default; the ADR stays open) | yes |
| Feature 19 (REQ-ACA-028): the Academics sheet has no aggregate, entity or folder. Slices SL-ACA-209 (phase 2) and SL-ACA-400 to 404 depend on "the Academics daily sheet OpenAPI from phase 2". Open point 1 says "No open question owns this" and scores it 4 | Completeness | 3 hours plus an Appendix B amendment | no |
| Understated open points: Hr open point 2 gives likelihood 2 to a certain outcome, and Academics open point 1 gives impact 2 to a signature feature's gated demo | Risk honesty | 30 minutes | no |
| `11-messaging-architecture.md:1061` calls TC-MSG-901 a UI test in doc 08, but it is a Platform-sheet integration test. Doc 11 open point 5 still leaves the spike "for document 34 to schedule", though SL-MSG-001 has it | Testability | 15 minutes | no |
| `notification.md:855` and `wellbeing.md:1014` cite TC-PLAT-009 (font shaping) as the no-Google proof, and `identity.md:2298`'s right-to-left row has no test id | Portability | 30 minutes | no |
