#### Group C

| Axis | Score | Evidence | What would raise it by one |
|---|---|---|---|
| Completeness | 3 | The round-3 gaps are closed. `11-messaging-architecture.md:490` binds `documents.commands` "also on ... `nibras.hr`, `nibras.behavior`, `nibras.operations`, `nibras.scheduling`". `bff-web.md:118` adds "`/bff/web/v1/campus/digital-twin?campusId=&date=`". `academics.md:250` adds "### 4.11 Tier 2: `DailySheet`". A new gap of the same kind remains on the notification side. `11-messaging-architecture.md:459` binds `notification.commands` "also on `nibras.admissions`, ... `nibras.ai`, `nibras.audit`", which leaves out Wellbeing, Communication, Reporting, Documents and Behavior. All five send `RequestNotification`. At `wellbeing.md:514`, `MissedDoseJob` alerts the "nurse at once" via `RequestNotification`. As written, that alert, the safeguarding escalations (`wellbeing.md:510`), the morning-brief push (`reporting.md:442`) and the export approvals (`documents.md:677`) have no queue. Two consumers are also missing. The Behavior sheet's §6.2 (`behavior.md:334`-`351`) has no consumer for the `behavior.events` binding at doc 11:506. Operations stores `document_id` (`operations.md:155`, `170`) but has no generated-event queue | Bind `nibras.wellbeing`, `nibras.communication`, `nibras.reporting`, `nibras.documents` and `nibras.behavior` on `notification.commands` in doc 11 §2.4 and §2.5, together with `nibras.hr` and `nibras.operations`, which are already recorded as open. Add Behavior's `documents.document.generated.v1` consumer row. Either bind the generated event for Operations or drop its `document_id` columns |
| Consistency | 3 | The round-3 contradictions are fixed. Feature 43 now reads the same in `hr.md:24` ("The rung 2 strain model runs in Hr") and in SL-HR-616. `wellbeing.md:1085` gives the default "the same as the Documents sheet's open point 13". The doc 11 change has left drift behind it. `behavior.md:923` still says "`documents.commands` does not bind `nibras.behavior`". `scheduling.md:1050` says doc 11 "does not list `nibras.scheduling` as a sender of `GenerateDocument`". `scheduling.md:1054` says the same for `RequestNotification`, which doc 11:459 now binds. `11-messaging-architecture.md:260` says Operations and Scheduling "keep no document id", yet `operations.md:155` stores "`po_number`, `promised_on`, `document_id`". `wellbeing.md:416` presents its alerts as sent, while doc 11 does not route them | Close Behavior open point 8 and Scheduling open points 5 and 9, and fix `scheduling.md:561`. Make doc 11:260 and the Operations `document_id` columns agree. Make Wellbeing §7 agree with doc 11 once the notification binding is added |
| Feasibility | 4 | `11-messaging-architecture.md:1137` says "Document 34 schedules it as the exit check of SL-MSG-001 in phase 1, read against the 5-second line". This closes the stale spike point. The digital twin's upstream reads exist in their own sheets: `attendance.md:344` has "`/api/v1/attendance/reports/daily-register?campusId=&date=`" and `scheduling.md:363` has "`/rooms/availability`". Residue: until `notification.commands` binds the missing senders, the Wellbeing and Reporting jobs cannot deliver as written | Add the notification bindings (see Completeness) |
| Risk honesty | 4 | The round-3 understatements are re-scored. `hr.md:1443` now reads "Certain until the binding exists ... \| 5 \| 2 \| 10 \| RISK-43". `academics.md:1023` says feature 19 "is gated on the amendment ... \| 3 \| 3 \| 9". It also says "No open question ... carries the amendment yet", which is honest. Residue: Wellbeing has no open point for the fact that `notification.commands` does not route its safety alerts. The only exposure named is Appendix C's missing rows (`wellbeing.md:1080`), so a hard part stays unnamed | Add a Wellbeing open point, scored and linked to a RISK, for the unrouted missed-dose and safeguarding alerts until doc 11 binds `nibras.wellbeing`. Enter the daily-sheet Appendix B amendment as an Open Question |
| Testability | 4 | `11-messaging-architecture.md:1064` now says "the replay-applies-once integration test behind the failed-messages screen is `TC-MSG-901` (Platform sheet)". This matches `platform.md:2322`. `bff-web.md:522` gives the new twin composer a test: "TC-BFF-027 ... No student name or id appears anywhere in the payload". Residue: `TC-WEL-034` (escalation to the principal) asserts a delivery that doc 11 cannot route today | Once the binding is added, add a doc 11 contract test that every sheet's `RequestNotification` sender is bound |
| Distinctiveness | 4 | All three round-3 feature gaps are specified. Feature 33: `bff-web.md:167` joins "Operations ... Scheduling ... Attendance ... no student row leaves the composer". Feature 19: `academics.md:413` says "The routes are specified in full, and only the permission waits". Feature 43: `hr.md:451` carries "the rung 2 strain suggestion (`assistRung = 2`) with its factors and weights". Residue: feature 25's morning-brief push (`reporting.md:442`) goes through an unbound `RequestNotification` | Bind `nibras.reporting` so that feature 25's push is deliverable |
| Portability | 4 | `identity.md:2298` now cites "`TC-NOT-604` and `TC-NOT-618` ... on `ubuntu-latest`". `notification.md:855` and `wellbeing.md:1014` cite "`TC-MOB-988` (document 20), the no-Google device-pass test". Residue: eight sheets still cite the font-shaping test as the no-Google proof. `attendance.md:1220` has "`TC-PLAT-009` (document 33), the device pass on one device without Google services". Doc 33:231 defines TC-PLAT-009 as "Font shaping differs between emulator and device". The same citation appears in `admissions.md:905`, `assessment.md:1136`, `audit.md:649`, `behavior.md:860`, `communication.md:922`, `platform.md:2370` and `requests.md:1004` | Replace TC-PLAT-009 with `TC-MOB-988` in the no-Google rows of those eight sheets |

**Verdict:** Blocked (blocking axes: Completeness, Consistency)

| Round-3 gap | Now | Evidence |
|---|---|---|
| About 34 brief-findings open; workarounds pending ADRs (round-2 row, Partly closed in round 3) | Partly closed | The sheet points that only doc 11 could fix are closed in doc 11 (`11-messaging-architecture.md:490`, `562`), and `documents.md:1372` marks open point 7 "Closed 2026-09-26". But `behavior.md:923` and `scheduling.md:1050` still say the binding is missing |
| Doc 11 §2.4/§2.5 do not route `GenerateDocument` from Behavior, Hr, Operations, Scheduling, nor bind the generated event on `hr.events` | Closed | `11-messaging-architecture.md:324` and `490` bind all four. Line 562 binds `documents.document.generated.v1` on `hr.events`, and `hr.md:514` has the consumer. New residue: there is no Behavior §6.2 consumer, and Operations has no queue for its `document_id` |
| Bff.Web has no digital-twin composer; Operations open point 10 says Attendance is not read | Closed | `bff-web.md:118`, `167`, `358`-`362` and `424` give the route, upstreams, tree and TC-BFF-027. `operations.md:1405` reads "Attendance roll-call counts from the daily register" |
| Feature 43: Hr says it runs no model; SL-HR-616 builds rung 2 in Hr | Closed | `hr.md:24` says "The rung 2 strain model runs in Hr ... SL-HR-616 builds it". `hr.md:1166` names `WorkloadStrainScorer.cs`, and `34-work-breakdown.md:1396` agrees |
| Wellbeing open point 11 and Documents open point 13 give different interim defaults (RISK-48) | Closed | `wellbeing.md:1085` has "Interim default, the same as the Documents sheet's open point 13 ... `DocumentRequestPublisher` is therefore not wired". `documents.md:1378` and doc 11:499 match it |
| Feature 19: Academics has no aggregate, entity or folder | Closed | `academics.md:250`-`267` (aggregate and invariants), `411`-`421` (routes), `557` (`DailySheetSendJob`) and `693`, `789` (folders). Only the permission is pending, at open point 1 |
| Understated open points: Hr open point 2 likelihood 2; Academics open point 1 impact 2 | Closed | `hr.md:1443` "\| 5 \| 2 \| 10 \| RISK-43"; `academics.md:1023` "\| 3 \| 3 \| 9" |
| Doc 11:1061 misplaces TC-MSG-901; doc 11 open point 5 leaves the spike to doc 34 | Closed | `11-messaging-architecture.md:1064` says "`TC-MSG-901` (Platform sheet)", and line 1137 says "Document 34 schedules it as the exit check of SL-MSG-001" |
| Notification and Wellbeing cite TC-PLAT-009 as the no-Google proof; Identity's RTL row has no test id | Partly closed | `notification.md:855` and `wellbeing.md:1014` now cite `TC-MOB-988`, and `identity.md:2298` cites `TC-NOT-604`/`TC-NOT-618`. Eight other sheets still cite TC-PLAT-009 as the no-Google device pass, e.g. `attendance.md:1220` |

| Gap | Axis | Effort to close | Blocking |
|---|---|---|---|
| `notification.commands` (doc 11:459, and the §2.4 row at doc 11:328) does not bind `nibras.wellbeing`, `nibras.communication`, `nibras.reporting`, `nibras.documents` or `nibras.behavior`, although all five send `RequestNotification`. As written, the missed-dose alert (`wellbeing.md:514`), safeguarding and guardian-contact escalations (`wellbeing.md:510`, `512`), meeting reminders, the morning-brief push and export approvals have no queue. Wellbeing records no open point for this. Hr and Operations do record it (open point 2, RISK-43) | Completeness | 1 hour in doc 11 §2.4 and §2.5, plus a Wellbeing open point if the binding is deferred | yes |
| Stale open points contradict the fixed doc 11. `behavior.md:923` (point 8) and `scheduling.md:1050` (point 5) say `documents.commands` does not bind them. `scheduling.md:1054` (point 9) and `561` say `nibras.scheduling` is not a `RequestNotification` sender. Scheduling has no endpoint that sends `GenerateDocument`, although doc 11:324 says it sends one for timetable PDFs | Consistency | 45 minutes | yes |
| `11-messaging-architecture.md:260` says Operations "keep[s] no document id", but `operations.md:155` and `170` store `document_id` on purchase orders and safety records, and no Operations queue binds `documents.document.generated.v1` | Consistency | 30 minutes | yes |
| Behavior §6.2 (`behavior.md:334`-`351`) has no consumer row or queue for the `behavior.events` binding of `documents.document.generated.v1` (doc 11:506), so the award certificate id Behavior "stores" has no handler | Completeness | 20 minutes | no |
| Eight sheets (admissions, assessment, attendance, audit, behavior, communication, platform, requests) cite `TC-PLAT-009`, the font-shaping check, as "the device pass on one device without Google services". They should cite `TC-MOB-988` | Portability | 30 minutes | no |
| `21-performance-engineering.md:281` lists "kindergarten daily sheets" under "Never cached in Operations", but the Academics sheet now owns them (`academics.md:250`). Doc 21 §1 for Academics does not name them, and no hot query covers `/daily-sheets?sectionId=&date=` | Consistency | 20 minutes | no |
| The daily-sheet Appendix B amendment is still not an Open Question (`academics.md:1023`: "entering one there is the next step for the product owner") | Risk honesty | 10 minutes | no |
| The round-4 edits added no review-record rows to the sheets they changed (the latest entries are the round-3 remediation, e.g. `hr.md:1460`) | Consistency | 15 minutes | no |
