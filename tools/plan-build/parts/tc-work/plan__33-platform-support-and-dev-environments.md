# Test-case work for `docs/plan/33-platform-support-and-dev-environments.md`

## Tests cited but defined nowhere: define them here

- **TC-PLAT-102**
  - cited `docs/plan/03-requirements-catalog.md` line 1104: "REQ-PLAT-022 | Every tool entry point ships a `.ps1` and a `.sh` wrapper over one Node implementation, and every hook invokes `node` with a relative path | 1 | cross-cutting | Appendix X.4 | none"
  - cited `docs/plan/07-solution-structure.md` line 902: "REQ-PLAT-022 | "Every tool entry point ships a `.ps1` and a `.sh` wrapper over one Node implementation, and every hook invokes `node` with a relative path" (03), as ADR-0017 decided | Every entry poin"
  - cited `docs/plan/07-solution-structure.md` line 906: "TC-PLAT-101 is the appliance build job in `release.yml`; TC-PLAT-102 is the template smoke test in `ci-kit.yml`. Both identifiers are owned by `03-requirements-catalog.md`, which carries them against "
  - cited `docs/plan/20-traceability-matrix.md` line 907: "REQ-PLAT-022 | Every tool entry point ships a `.ps1` and a `.sh` wrapper over one Node implementation, and every hook invo... | 1 | cross-cutting | none | none | 33 | 1 | SL-PLAT-001 | per Appendix X "
