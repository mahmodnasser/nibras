#### Group C

| Axis | Score | Evidence | What would raise it by one |
|---|---|---|---|
| Completeness | 3 | Eleven data-owning sheets still have no `stateDiagram`; the saga orchestrators still defer: `06-services/platform.md:659` "The saga designs, steps, compensations, timeouts ... are `13-workflows-and-sagas.md` Saga 1, Saga 2 and Saga 10". Numbering still drifts: `identity.md:28`, `platform.md:28`, `audit.md:28`, `gateway.md:27` open at "## 2. Responsibilities". New: the Bff.Web sheet has no row for the three internal routes Ai's jobs call, which `05-service-catalog.md:45` requires: "`GET /bff/web/v1/internal/ai/sources/{sourceEntity}?cursor=` ... `POST /bff/web/v1/internal/ai/sources/authorize` ... `POST /bff/web/v1/internal/ai/tools/{toolName}`". A grep for `internal/ai` in `bff-web.md` finds nothing, so Bff.Web cannot be built from its sheet (PLAN_SPEC rule 3) | Add the three internal Ai routes, with credential, permission and budget, to `bff-web.md` §4.1 and its tree. Add state diagrams (or embedded copies of doc 13's) to Platform, School, Admissions and the other workflow owners. Normalise section numbering |
| Consistency | 3 | The three named round-1 contradictions are fixed in docs 10 and 21 (`21-performance-engineering.md:717` "Money columns are numeric(18,4)"). A new three-way contradiction on Ai indexing: `06-services/ai.md:361` "Appendix E (v9.1) names Ai in the consumer column of the five source change keys", against `11-messaging-architecture.md:1114` "No: Appendix E names no such binding" and `10-data-architecture.md:319` "Indexing reads through the backends-for-frontends only". Doc 11 §2.5 has no `ai.index-sources.bulk` queue. Sheets still report fixed defects as open: `identity.md:2204` "§2.3 gives `svc_gateway` no read on it", yet `21-performance-engineering.md:391` grants "read-only `~state:revoked:*`". `finance.md:1601` still says doc 21 "writes money as `numeric(19,4)`". `ai.md:491` still reads "in place of document 21's `embeddings` and `scope_code`". `bff-web.md:114` answers natural-language search with "Answer with sources", against `ai.md:253` "202 job" | Bind the five source change keys and `school.student.section-changed.v1` on `ai.index-sources.bulk` in doc 11 §2.5. Close doc 11 open point 7 and fix doc 11:578 and doc 10:319. Close identity open point 9, gateway open point 1, finance open point 9 and line 877, and ai.md:491 and open point 5. Make the Bff.Web natural-language route return the 202 assist job |
| Feasibility | 4 | The round-1 blocker is gone. `21-performance-engineering.md:372` adds "`state:revoked:{tenant}:{userId}` \| Identity writes, Gateway reads", and the ACL at line 391 grants the read. Unknowns carry owners and defaults, e.g. `11-messaging-architecture.md:1112` "Yes, until phase 6 load tests measure `nibras_outbox_oldest_pending_seconds` above 5 s". Still, Ai's indexing depends on Bff.Web routes that no sheet specifies (see Completeness), and dispatcher capacity at peak stays unmeasured until phase 6 | Specify the Bff.Web internal Ai routes. Bring forward a dispatcher throughput spike (a single-leader outbox at the Appendix N peak) to the phase 1 foundation slices rather than phase 6 |
| Risk honesty | 4 | Docs 10, 11 and 21 now have risk sections with likelihood, impact, mitigation and owner, e.g. `10-data-architecture.md:579` "A pooled connection under PgBouncer transaction mode serves one tenant's rows to another ... \| 3 \| 5". The Wellbeing default is stated honestly, `06-services/wellbeing.md:1063` "until the privacy officer decides, this default contradicts the rule", scored 20 and linked to RISK-47. Residue: seven rows say "none yet; ... proposes it at the Group E review" (`11-messaging-architecture.md:1063`, "A message is lost between the state change and the broker"). Group E is done and doc 18 holds none of them. The sheets' own risk tables still use "low \| critical" (`wellbeing.md:1013`), not doc 18's 1-to-5 scale | Enter the seven proposed rows in doc 18, or state why they stay local. Convert the sheet risk tables to the 1-to-5 L and I of doc 18 |
| Testability | 3 | Sheets map claims densely, but doc 11 still has one TC id (TC-MSG-901). Its guarantees are class names only: `11-messaging-architecture.md:1051` "`LaneIsolationTests.BulkBacklog_UrgentStillWithin30Seconds`". New from the theme-5 renumbering: `21-performance-engineering.md:1031` cites "the pooled-connection test `TC-DATA-001` to `TC-DATA-003`". But `16-annex-test-case-registry.md:457` defines TC-DATA-001 as "Uploaded to Parsed / File matches a known template version". `10-data-architecture.md:581` cites `TC-DATA-006` (import rollback) for reconciliation, and line 666 reads "`TC-DATA-643` to `TC-DATA-006` are the WF-DATA-01 transitions". R20 passes because the ids exist; the meaning is wrong. `attendance.md:1123` still asserts "alert enqueued within 30 s" for TC-ATT-003 while Open Question 27 is open | Register a TC id for every doc 11 suite in doc 16 and its annex. Replace TC-DATA-001..003 with TC-DATA-643..645 at doc 21:1031, 1250 and 1315, and fix doc 10:581 and 666. Mark TC-ATT-003's 30 s clause conditional on Open Question 27 |
| Distinctiveness | 3 | Unchanged since round 1. "signature feature" and "Appendix O" still occur 0 times in docs 10, 11 and 21 and in 21 of 23 sheets. Theme 8 put the demo tests in documents 15, 16 and 32, not here. The mechanisms stay sharp but untied to features, e.g. `21-performance-engineering.md:1183` "Warm-up and scale-up run on each tenant's own school calendar". No sheet names the feature, minute or demo test the mechanism serves | Add a "Signature features served" table per sheet and to docs 11 and 21, naming the feature, its Appendix O moment and its SL-TST-006 demo test |
| Portability | 3 | Unchanged. "Linux" occurs 0 times in 22 of 23 sheets and in docs 10 and 21; only `11-messaging-architecture.md:1037` says "on Windows and Linux runners". Devices without Google services are covered only in `notification.md:818` "A device without Google services receives push in-app while open". Attendance, Wellbeing, Gateway and Identity have 0 RTL mentions. Doc 10 names Arabic normalisation (`10-data-architecture.md:61` "`pg_trgm` and `unaccent` where Arabic search normalization runs") but no ICU collation, time-zone or runner proof | Add a portability row to each sheet's test plan (culture-invariant parsing, Arabic collation and search, RTL of generated documents, no-Google mobile paths) naming the runner that proves each. Add the same to docs 10 and 21 |

**Verdict:** Blocked (blocking axes: Completeness, Consistency, Testability, Distinctiveness, Portability)

| Round-1 gap | Now | Evidence |
|---|---|---|
| Ai index named `embeddings`/`scope_code` in docs 10 and 21 | Partly closed | Fixed at `10-data-architecture.md:266` and `21-performance-engineering.md:983` "`ai_index.embedding_chunk`, its columns and `ix_chunk_scope`". But `06-services/ai.md:491` still reads "in place of document 21's `embeddings` and `scope_code` (open point 7)" |
| Doc 21 Redis ACL lacks `state:revoked:` and the Gateway reads | Closed | `21-performance-engineering.md:391` "read-only `~state:revoked:*` (Section 2.2)" plus the key set, maintenance and settings. Stale open points remain at `identity.md:2204` and `gateway.md:414` (new gap) |
| Finance "Resolved" note vs doc 21 `ux_series_code` and `numeric(19,4)` | Closed | `21-performance-engineering.md:722` "`ux_series_campus_code ON finance.series (tenant_id, campus_id, code)`"; line 717 "numeric(18,4)". But `finance.md:1601` open point 9 still reports the old defect |
| No risk register in docs 10, 11, 21 | Closed | `10-data-architecture.md:573`, `11-messaging-architecture.md:1057`, `21-performance-engineering.md:1242` "Risks this document carries", with L, I, mitigation and owner. Seven rows are still unregistered (new gap) |
| No link from mechanisms to signature features; feature-39 demo gap | Open | 0 hits for "signature feature" or "Appendix O" in docs 10, 11, 21 and 21 of 23 sheets. Feature 39 moved to engineering by ADR-0019 (PROJECT_STATE:37), but doc 21 §9 still names no feature |
| Sheets never name runner, OS, RTL or no-Google behaviour | Open | "Linux" 0 times in 22 sheets and docs 10 and 21; no-Google only in `notification.md:818` |
| About 34 brief-findings open; workarounds pending ADRs | Partly closed | ADR-0019 closed many (e.g. `finance.md:1595` "Resolved 2026-09-22 (ADR-0019): Appendix B carries `finance.payers`"). 37 "Still open" points remain, scored and owned, and the Ai indexing fix in Appendix E has not reached docs 10 and 11 |
| Saga orchestrators and 8 other workflow owners lack state diagrams | Open | 0 `stateDiagram` in 14 sheets, including platform, school, admissions; `platform.md:659` still defers to doc 13 |
| Doc 11 suites lack TC ids; TC-ATT-003 30 s depends on the pending decision | Open | Doc 11 cites only TC-MSG-901. `attendance.md:1123` "alert enqueued within 30 s" is unconditional, though `attendance.md:1246` scores the decision and links RISK-41 |
| Wellbeing device outbox has no risk row | Closed | `wellbeing.md:1063` is scored 4, 5, 20 with "RISK-47". `18-risk-register.md:120` carries RISK-47. `wellbeing.md:614` states the contradiction openly |

| Gap | Axis | Effort to close | Blocking |
|---|---|---|---|
| Ai source-change indexing: `ai.md:361` and Appendix E bind five change keys on `ai.index-sources.bulk`. Doc 11 §2.5, line 578 and open point 7, and doc 10:319, say no binding exists and indexing is BFF-read only | Consistency | 2 hours | yes |
| Bff.Web sheet omits the three internal Ai routes (source feed, authorize, tools) that docs 05, 25 and the Ai sheet require. Its natural-language search returns a synchronous answer where Ai returns a 202 job | Completeness | 3 hours | yes |
| Theme-5 renumbering left wrong meanings behind valid ids. Doc 21:1031, 1250 and 1315 call TC-DATA-001..003 (import-template transitions) the pooled-connection tests. Doc 10:581 cites TC-DATA-006 for reconciliation, and doc 10:666 has the range "TC-DATA-643 to TC-DATA-006" | Testability | 1 hour | yes |
| Doc 11 messaging guarantees (chaos, deliver-twice, ordering, lanes, fairness, pacts) have class names but no TC ids | Testability | 4 hours | yes |
| Stale open points contradict the fixed doc 21: identity open point 9, gateway open point 1, finance open point 9 and line 877, ai.md:491 and ai open point 5 (doc 11:328 already lists `nibras.ai` as a `RequestNotification` sender) | Consistency | 1 hour | yes |
| No sheet or architecture document ties a mechanism to a signature feature's moment, rung and demo test | Distinctiveness | 1.5 days | yes |
| No runner, OS, RTL or no-Google coverage named in 22 sheets or in docs 10 and 21 | Portability | 2 days | yes |
| 14 sheets without a state diagram, including all three saga orchestrators; four sheets start at section 2 | Completeness | 1 day | yes |
| Seven risk rows in docs 10, 11 and 21 were "proposed at the Group E review" but never entered doc 18; sheet risk tables use low/med/high instead of doc 18's 1-to-5 scale | Risk honesty | 2 hours | no |
| TC-ATT-003's "within 30 s" clause in the Attendance sheet goes beyond its registry definition ("delivered or deferred") and is not marked conditional on Open Question 27 | Testability | 30 minutes | no |
