#### Group E

| Axis | Score | Evidence | What would raise it by one |
|---|---|---|---|
| Completeness | 4 | Every PLAN_SPEC item for this group is now present. 15 sends capacity and cost to one owner: `15-deployment-and-operations.md:796` "`28-capacity-and-cost-model.md` owns every sizing number, threshold and price". 19 has the FsCheck row: `19-dependency-and-license-inventory.md:101` "FsCheck \| ... \| 3.4.0 \| BSD-3-Clause". 17 has CAP-INT-02 and CAP-INT-03 (`17-roadmap.md:202`, `:221`). One required item is incomplete: 19's "scanner allow-list" is quoted as `19-dependency-and-license-inventory.md:310` "Seven entries, quoted as the file stands on 2026-09-22". `tools/license-scan/allow.json` has eight entries, and the k6 entry is missing from the quoted table. | Re-quote the allow-list from the file as it stands, with the k6 row and its ADR (0019). |
| Consistency | 3 | New contradictions, several of them introduced by the remediation. (1) 19 contradicts itself on k6: `19-dependency-and-license-inventory.md:364` "the `allow.json` entry is outstanding (open point 1)", against `:388` "1. Closed: k6 ... has ... its `allow.json` entry". (2) 17 claims more than Appendix O allows: `17-roadmap.md:204` "The full Appendix O script passes on a freshly provisioned tenant" in phase 4. `appendix-o-demo-script.md:60` gives minute 12 "Phase 5", and `16-test-strategy.md:632` runs "minute 12 as scripted" only in phase 5. (3) CASE is built twice. `20-traceability-matrix.md:204` has REQ-ACA-007 "imported from CASE", phase 2, SL-ACA-203; `17-roadmap.md:223` makes "a CASE import" a phase 5 exit criterion. (4) `17-roadmap.md:111` "its 156 service requirements", against `:25` "153". (5) `15-deployment-and-operations.md:863` "`16-test-strategy.md`, which registers them", against `:913` "This document defines the operations tests below". (6) `15-deployment-and-operations.md:204` "`tests/load/`", where `16-test-strategy.md:132` and `07-solution-structure.md:576` have `Load/`. | Make 19 say one thing about k6. Restate the 17 phase demos and exit criteria using the Appendix O phase column and its substitutions. Give CASE import one phase. Fix 17:111, 15:863 and the `tests/load/` path. |
| Feasibility | 3 | The schedule is now derived from the slices, with the arithmetic shown: `17-roadmap.md:45` "slice-days × 1.3 ÷ (builders × 5)", and `:25` puts phase 1 at "14 to 22 weeks". Three problems remain. First, the builders include the tester: `17-roadmap.md:43` "5 to 8 \| ... one quality engineer", yet the same person runs the device pass, acceptance and the demo gate. Second, the formula has no allowance for holidays or Ramadan. Third, the load tier is still over-scheduled. `16-test-strategy.md:499` runs the 24-hour soak "nightly on the load tier", `:578` says that tier is "rebuilt nightly", and `:503` runs chaos there nightly too. A 24-hour run with a 2-hour tail cannot fit a nightly rebuild. | Give the soak its own environment or a weekly slot on the load tier. Take the quality engineer out of the builders input, or show the capacity it keeps back. State the calendar allowance. Then recompute with `schedule-34.mjs`. |
| Risk honesty | 3 | The register is complete and its arithmetic checks: `18-risk-register.md:172` "the four bands add to 52", and the top ten recompute correctly. Where it matters, it is optimistic. `18-risk-register.md:78` says "a slice whose requirement is Tier 2 cannot enter a Tier 1 phase without an ADR". Yet 20 schedules 20 Tier 2 requirements into phases 1 and 2, for example `20-traceability-matrix.md:204` "REQ-ACA-007 ... \| 2 \| ... \| 2". The top-ten action (`:185`) "Make the tier check part of `/lint-plan`" is dated "Before document 34 is written"; that date has passed, and no kit-lint rule does it. `:86` watches for "a brief version other than v9.1", but the brief is v9.4. RISK-44 (`:87`) leaves out ADR-0020 to ADR-0023, which are Proposed and which brief v9.2 to v9.4 rest on. `:79` gives RISK-03 the status "Occurred and treated", which is not one of the four statuses at `:29`. | Either write the ADR for the 20 Tier 2 requirements in the MVP or move them, and build the tier check or re-date it honestly. Re-anchor RISK-43 on v9.4 and ADR-0020 to ADR-0023, add those ADRs to RISK-44, and give RISK-03 a valid status. |
| Testability | 4 | Every requirement has an identifier, and each test is defined once: `16-test-strategy.md:797` "Kit-lint R20; R23 fails when `16-annex-test-case-registry.md` differs". The verification rows are now honest about what is only reviewed: `16-test-strategy.md:798` "Review step, not a lint rule". Gaps: 333 requirements rest on derived identifiers, specified only by Given, When, Then lines (`20-traceability-matrix.md:30` "`TC-<AREA>-(950 + requirement number)`"). One drill has two test ids: `15-deployment-and-operations.md:514` "`TC-DATA-020` on the load tier", with the Environment cell reading "Staging", while `16-test-strategy.md:524` and `18-risk-register.md:146` use `TC-TST-216`. `16-test-strategy.md:81` says 20 "is generated from those traits", against `20-traceability-matrix.md:5` "generated from documents 03, 17, 31 and 34". | Give the single-tenant restore drill one test id in 15, 16 and 18 and fix the Staging and load-tier conflict. Correct 16:81. Name the slice that writes each derived test. |
| Distinctiveness | 4 | These are support documents, so the axis is scored on how well they back the signature features. They now do: `16-test-strategy.md:635` "the demo test of every one of the 43 signature features runs in every release gate", checked by R34, and `15-deployment-and-operations.md:205` makes a missing demo test block promotion. Feature 39 is settled (`appendix-w-feature-register.md:7`). What remains is the roadmap's demo promise: `17-roadmap.md:28` "The full Appendix O script on a fresh tenant", which minute 12 cannot meet before phase 5. | Make each 17 phase demo name the Appendix O minutes it runs as scripted and those it runs by substitution. |
| Portability | 4 | Runners and devices are named: `16-test-strategy.md:451` "one device without Google services, per Appendix X.2", and `15-deployment-and-operations.md:300` "`ubuntu-latest` and `windows-latest`, one Podman configuration and one Docker Engine configuration". Every round-1 gap is still open. `15-deployment-and-operations.md:119` claims "macOS 14" with no runner behind it. `:518` drills only "A physical Hyper-V host", although `:543` ships a VMware `.ova`. `20-traceability-matrix.md:886` to `:908` still say "per Appendix X". New: `15-deployment-and-operations.md:204` and `:708` "`tests/load/`" point at a folder spelled `Load/` in documents 07 and 16, which fails on a case-sensitive file system. | Name the runner in 20's Platform column, prove or drop macOS 14, add a VMware drill, and fix the path case. |

**Verdict:** Blocked (blocking axes: Consistency, Feasibility, Risk honesty)

| Round-1 gap | Now | Evidence |
|---|---|---|
| Phase 1 range (8 to 10 weeks) against 423 slice-days; MVP line to recompute | Closed | `17-roadmap.md:25` "14 to 22 weeks"; `:244` "42 capabilities, 954 slice-days, and 33 to 50 weeks"; `:351` kit-lint R23 recomputes them. One stale count is left, `:111` "156" |
| 17 inconsistent with itself and with 20: workflow counts, INT placement, CAP-INT-02 and CAP-INT-03 missing | Closed | `17-roadmap.md:32` "52 distinct `WF-` identifiers"; `:60`, `:78`, `:83` split INT across phases 1, 3 and 4; `:202` and `:221` add CAP-INT-02 and CAP-INT-03. A new CASE double-build is listed below |
| "How this document is verified" rows cite `/lint-plan` checks that do not exist | Closed | `18-risk-register.md:258` names R24 and says what `plan-consistency-checker` checks; `16-test-strategy.md:798` "Review step, not a lint rule"; `17-roadmap.md:348` cites R25 and R19 |
| TC ids cited in 15 and 18 missing from 16 and 20; the TC-PLAT and TC-DOC collisions | Partly closed | R20 is clean, and `15-deployment-and-operations.md:921` defines TC-INF-105. But `:863` still says 16 "registers" TC-INF-101 and above, which `:913` contradicts |
| Stale register: RISK-03, RISK-07, RISK-34; no risks for Ai, k6, pending decisions or appendix defects | Closed | `18-risk-register.md:79` RISK-03 "Occurred and treated"; `:83` "week 1 of Phase 2"; `:146` drills in phase 6; RISK-38 to RISK-43 added. New defects in RISK-02, RISK-03 and RISK-43 are listed below |
| k6 AGPL not allowed; the load tier over-scheduled | Partly closed | `allow.json` now has the k6 entry and `18-risk-register.md:137` closes RISK-40, but `19-dependency-and-license-inventory.md:364` still says the entry is "outstanding". The soak is still nightly on a tier rebuilt nightly (`16-test-strategy.md:499`, `:578`) |
| Drift: phase service counts, N-06 timing, versions, runbook count, scanner test count | Closed | `16-test-strategy.md:622` "Plus 4 \| Plus 3 \| Plus 4"; `:628` N-06 "nightly from phase 3"; `:19` "PostgreSQL 18.6 ... Valkey 9.1.2"; `15-deployment-and-operations.md:693` "Sixty-three"; `19-dependency-and-license-inventory.md:318` "12 tests"; `run.test.mjs` has 12 |
| 16 claims every signature feature has an Appendix O row; feature 39 unsettled | Partly closed | `16-test-strategy.md:661`, backed by R34, and feature 39 is moved (`appendix-w-feature-register.md:7`). The 17 phase demo claims still conflict with Appendix O: `17-roadmap.md:204` |
| 15 has no capacity and cost model; 19 has no FsCheck row | Closed | `15-deployment-and-operations.md:794` part 13; `19-dependency-and-license-inventory.md:101` FsCheck 3.4.0 |
| 20's Platform column says "per Appendix X"; macOS unproven; no VMware drill | Open | `20-traceability-matrix.md:886` "per Appendix X"; `15-deployment-and-operations.md:119` "macOS 14"; `:518` "A physical Hyper-V host" |

| Gap | Axis | Effort to close | Blocking |
|---|---|---|---|
| 19 says the k6 `allow.json` entry is outstanding (§8, §9.1, §11.1, §11.3, Decisions) and quotes seven entries, while the file has eight and 19's open point 1 and RISK-40 say it is closed | Consistency | 1 hour | yes |
| 20 Tier 2 requirements (REQ-IDN-010, REQ-ACA-007, REQ-ASM-010 and others) sit in phases 1 and 2 with no ADR, against RISK-02's own mitigation; RISK-02's top-ten tier check was due "Before document 34 is written" and no rule does it | Risk honesty | 0.5 day | yes |
| 17 promises "the full Appendix O script" at phase 4 (`:28`, `:204`), and the acts at phases 2 and 3 without substitutions, against the Appendix O phase column and `16-test-strategy.md:632` | Consistency | 1 hour | yes |
| CASE import is built twice: REQ-ACA-007 in phase 2 by SL-ACA-203, and REQ-INT-016's CASE import as a phase 5 exit criterion under CAP-INT-03 | Consistency | 2 hours | yes |
| RISK-43 watches for "v9.1" while the brief is v9.4; RISK-44 leaves out the Proposed ADR-0020 to ADR-0023 that brief v9.2 to v9.4 rest on; RISK-03's status is outside the four allowed values | Risk honesty | 2 hours | yes |
| The 24-hour soak runs nightly on a load tier that is rebuilt nightly and also carries every nightly gate and chaos; the builders input counts the quality engineer and allows nothing for holidays or Ramadan | Feasibility | 0.5 day | yes |
| Stale pointers: `17-roadmap.md:111` "156"; `15-deployment-and-operations.md:863` TC-INF owner; `:882` still lists 34's "sixty" as blocking, though 34 now says sixty-three; `:514` Staging against the load tier; the restore drill as TC-DATA-020 in 15 and TC-TST-216 in 16 and 18; the 15 and 16 "Last updated" dates predate the theme 8 edits | Consistency | 2 hours | yes |
| `tests/load/` in `15-deployment-and-operations.md:204` and `:708`, against `Load/` in documents 07 and 16 | Portability | 10 minutes | no |
| macOS 14 developer support unproven, no VMware drill, 20's Platform column says "per Appendix X" for REQ-PLAT-001 to 023 | Portability | 2 hours | no |
| 333 derived acceptance tests exist only as Given, When, Then lines; `16-test-strategy.md:81` misstates how 20 is generated | Testability | 1 hour | no |
