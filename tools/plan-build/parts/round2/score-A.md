#### Group A

| Axis | Score | Evidence | What would raise it by one |
|---|---|---|---|
| Completeness | 4 | Document 00 is still unwritten, which is by design (`PLAN_SPEC.md:117` "then update 00"). 01 now carries all 30 open questions, and R26 passes. 02 has an Open points section (`02-competitive-gap-analysis.md:147` "## Open points"). There are two thin spots. 01 says it restates the register (`01-questions-and-assumptions.md:3` "Every open point in `docs/project/OPEN_QUESTIONS.md` restated"), but it leaves out the four ADR-0019 value decisions under `OPEN_QUESTIONS.md:53` "## Settled in v9.1". And 01 gives no ordering for questions 29 and 30 against the workshop (`01:5` lists "Six questions (17, 19, 21, 22, 27, 28)"). | Add the four "Settled in v9.1" rows (cooling-off, invitation validity, dedupe window, feature 39) to 01 section 6. List 29 and 30 among the off-agenda questions, and say who takes them and when. Write 00 after Group F. |
| Consistency | 3 | There are new contradictions, several of them introduced by the remediation. (1) Phase 1 against the derived schedule: `01:113` says "A larger team does not shorten Phase 1", but `OPEN_QUESTIONS.md:32` says "phase 1 is 14 weeks with eight builders and 22 with five"; `01:57` says "except Phase 1, which is sequential". (2) The OneRoster phase: `01:18` says "Phase 4 (OneRoster and LTI)", while `01:47` puts OneRoster in "Phase 3", and `34:899` builds SL-INT-407 in phase 3. (3) The review guide is stale: `01:5` says it "still counts twenty-six", but `REVIEW_GUIDE.md:17` now says "28 questions, the last two settled", while `OPEN_QUESTIONS.md:35-36` has them Open. (4) The Q29 default: `01:48` says "The plan builds the encrypted outbox", yet its own mitigation says "build online-only first", and `18-risk-register.md:182` has "online-only check-in as the default". | Rewrite `01:57` and `01:113` from document 17's derived ranges (14 to 22 weeks). Change Q26's phases to match document 34 (OneRoster phase 3, LTI phase 4, CAP-INT-03 phase 5). Correct REVIEW_GUIDE to 30 open questions, with 27 to 30 open, and drop the stale sentence in `01:5`. Pick one Q29 default and state it the same way in 01, OPEN_QUESTIONS, 06 Wellbeing, 18 and 34. |
| Feasibility | 3 | Some impacts are now tied to the derived schedule (`01:57` "`tools/plan-build/schedule-34.mjs` recomputes the ranges from the real team"). Others are still unsized: `01:42` "the cost is measured in weeks, not days", and `01:43` adds nursery "to Phase 2" with no slice-days. The competitor re-check still belongs to a tool and has no effort or date: `02:142` "Owner: domain expert agent; timing: before the general-availability re-check" (the timing is circular). `02:152` names the product owner as commissioner but gives no phase or effort. | Size Q4, Q10 and Q28 in slice-days and weeks from document 34 (for example, CAP-INT-01's slice-days if it is pulled into the MVP). Give the re-check a human owner, a phase (for example, the Phase 6 entry) and an effort in person-days, and make `02:142` agree with `02:152`. |
| Risk honesty | 3 | Every question now carries L, I, a score, a mitigation and a RISK link (`01:7` "**Score** is L x I; **In the register** names the RISK identifiers"). `02:137` now points to RISK-31. However, 01 states as settled decisions whose records are still Proposed: `01:80` "A decision record closed each one; the plan states the decision as fact". All six cited records say otherwise (`DECISIONS/0003-seeded-admin-is-platform-only.md:3` "Proposed, awaiting product owner confirmation"; the same for 0002, 0009, 0010, 0011, 0012). `01:139` "One is settled: item 9, by ADR-0003". 01 even contradicts itself on ADR-0002 (`01:87` settled, `01:132` "ADR-0002, Proposed"). | Recast section 6 as "Proposed, default in force" with the product owner as owner, or have the product owner accept those six ADRs first. Restate the section 8 summary so that nothing rests on a Proposed record as settled. |
| Testability | 3 | R26 now exists and does what `01:147` claims (`kit-lint.mjs:942` "Document 01 and OPEN_QUESTIONS.md carry the same open questions"). But 02 still cites the wrong proofs for two signature claims. `02:66` proves exception-only attendance with TC-ATT-003, which `16-annex:248` defines as "Marked to AbsenceAlerted", while Appendix W names TC-ATT-812 (`appendix-w:44`). `02:40` and `02:68` prove smart defaults with TC-PLT-003, which `appendix-r:429` defines as "Owner invitation sent, tenant marked provisioned"; W row 29 has TC-PLT-802. `02:167` also misattributes both to "Appendix W". | Replace TC-ATT-003 with TC-ATT-812 at `02:38` and `02:66`, and TC-PLT-003 with TC-PLT-802 at `02:40` and `02:68`. Fix the source list at `02:167`. Where a claim is about webhooks specifically, cite the webhook assertion that SL-INT-002 tests, next to TC-INT-001. |
| Distinctiveness | 3 | The count of unmatched capabilities is now correct and each is tied to a feature (`02:55` "**Six** capabilities have no yes and no partial"; "Five of the six are carried by a signature feature"). Claims are reworded against evidence (`02:65` "**Reworded.** 'An Arabic interface' is table stakes"). But no signature feature in 02 carries its moment or rung, only a test id, and two of the five proofs RISK-31 relies on (features 26 and 29) point to tests that do not prove those features (see Testability). | Beside each "yes by commitment" cell for features 12, 24, 26, 27, 29, 31 and 32, add the Appendix W moment, the rung and the Appendix O minute. Correct the two test ids so that each sixty-second proof names its own demo test. |
| Portability | 4 | 01 names platform fallbacks and runners: `01:58` "Hosted macOS runner minutes", `01:114` "The `dev-smoke` job runs one Podman and one Docker configuration", `01:59` a Linux appliance for Windows hosts, and `01:31` a push fallback for devices without Google services. Round-1 gaps persist: no runner or device lab is named for the build without Google services (`01:31` cites only "33 (Android without Google services target)"), and 02 has no competitor row for mobile platforms or devices without Google services. | Name the CI runner or device that proves the build without Google services, and the AppGallery path, in Q17. Add a row to 02's matrix for iOS, Android and Android without Google services. |

**Verdict:** Blocked (blocking axes: Consistency, Feasibility, Risk honesty, Testability, Distinctiveness)

| Round-1 gap | Now | Evidence |
|---|---|---|
| The API count ("seven of ten") contradicted the matrix | Closed | `02:69` "Six of ten products publish an application programming interface"; `32-product-differentiation-and-demo.md:97` "Six of ten products publish one"; the matrix at `02:44` has six "yes" cells |
| `02:143` put Attendance in Phase 1 | Closed | `02:143` "and Attendance ... in Phase 2, which is the MVP cut line" |
| Questions 27 and 28 were missing from 01 and OPEN_QUESTIONS | Closed | `OPEN_QUESTIONS.md:35-36`, and `01:46-47` with a default, an owner, L/I and RISK-41/RISK-42. A new defect follows from this: REVIEW_GUIDE calls them "settled" (`REVIEW_GUIDE.md:17`) |
| `01:143` claimed a lint comparison that did not exist | Closed | `01:147` cites R26, and `kit-lint.mjs:942-957` compares the question numbers; the default comparison is honestly labelled as a review step by `plan-consistency-checker`, which exists in `.claude/agents/` |
| TC-INT-001 was cited as the webhook proof; TC-PLT-003 collided | Partly closed | TC-INT-001 is now Appendix W's feature 24 demo test (`16-annex:784`), and TC-PLT-003 has one meaning (`appendix-r:429`). But 02 still cites TC-PLT-003 for smart defaults (`02:40`, `02:68`), and it also cites TC-ATT-003 for exception-only attendance (`02:66`) |
| Section 8 summary misclassified items; .NET owner conflict | Closed | `01:139` "One is settled ... Three carry a Proposed ADR ... The remaining nine"; `01:66` explains the .NET owner. A new defect replaces it: item 9 is "settled" on a Proposed ADR |
| No likelihood, mitigation or RISK link in 01; "when written" was stale in 02 | Closed | `01:13` header includes "L \| I \| Score \| In the register"; each Evidence cell ends with "Mitigation:"; `02:137` "Recorded as RISK-31 in `18-risk-register.md`" |
| The "unmatched" count left out offline; no moment or rung per signature feature | Partly closed | The count is fixed (`02:55` "**Six** capabilities"). No moment or rung appears anywhere in 02 |
| The competitor re-check was agent-owned with no effort or date; impacts not in slice-days | Partly closed | `02:152` makes the product owner commission it. But `02:142` still says "Owner: domain expert agent", with no effort or phase, and `01:42` still says "weeks, not days" |
| Document 00 not written | Open | No `docs/plan/00-*.md` exists; this is by design, after Group F (`PLAN_SPEC.md:117`) |

| Gap | Axis | Effort to close | Blocking |
|---|---|---|---|
| Section 6 and item 9 of 01 state six decisions as settled "as fact" whose ADRs (0002, 0003, 0009, 0010, 0011, 0012) are "Proposed, awaiting product owner confirmation"; 01 contradicts itself on ADR-0002 (`01:87` against `01:132`) | Risk honesty | 1 hour (reword), or the product owner accepts the six ADRs | yes |
| 02 cites the wrong proofs for two signature claims: TC-ATT-003 (absence alert) for exception-only attendance instead of TC-ATT-812, and TC-PLT-003 (provisioning) for smart defaults instead of TC-PLT-802; `02:167` misattributes both to Appendix W | Testability | 0.5 hour | yes |
| 01 contradicts the derived schedule: "A larger team does not shorten Phase 1" (`01:113`, and `01:57`), against 14 weeks with eight builders and 22 with five (`OPEN_QUESTIONS.md:32`, `17-roadmap.md:333`) | Consistency | 0.5 hour | yes |
| Q26 puts OneRoster in Phase 4 (`01:18`); Q28 (`01:47`) and document 34 (`SL-INT-407`, phase 3) put it in Phase 3 | Consistency | 0.5 hour | yes |
| REVIEW_GUIDE says "28 questions, the last two settled", while 27 to 30 are Open. `01:5` describes it as "still counts twenty-six" and omits 29 and 30 from the off-agenda list, so the highest-scoring open decision (Q29, score 20) has no stated place in the product-owner review | Consistency | 1 hour | yes |
| The Q29 default is stated two ways: 01 and 34 build the device outbox, while 01's own mitigation and `18:182` make online-only the default | Consistency | 1 hour | yes |
| No moment, rung or Appendix O minute for any signature feature in 02 | Distinctiveness | 2 hours | yes |
| The competitor re-check has an agent owner, a circular timing and no effort (`02:142`); Q4, Q10 and Q28 impacts are not sized in slice-days | Feasibility | 3 hours | yes |
| 01 omits the four "Settled in v9.1" decisions that OPEN_QUESTIONS lists | Completeness | 0.5 hour | no |
| No runner is named for the build without Google services; 02 has no mobile-platform row | Portability | 1 hour | no |
