#### Group B

| Axis | Score | Evidence | What would raise it by one |
|---|---|---|---|
| Completeness | 4 | 03 now has both closing sections: `03-requirements-catalog.md:1107` "## Open points" and `03:1119` "## How this document is verified". The Summary matches the rows: a recount gives 758 / 94 / 8 = 860, as `03:53` states. 04 still relies on decisions that have no ADR, now with owners: `04-architecture-overview.md:554` "Decisions this document relies on that have no ADR yet". The 04 decision list stops at ADR-0017: `04:603` "The seventeen ADRs in Section 8 are Proposed". It leaves out ADR-0019, which is Accepted and rewrote reference architecture Section 8.0, and 04:328-330 rest on that section. The 07 tree in 2.2 does not draw the building-block test projects that `07-solution-structure.md:27` counts ("the 13 building-block test projects part 2.2 requires"). | Add ADR-0018 to ADR-0023 to 04 Section 8 with their real statuses, and cite ADR-0019 at 04:328. Draw the 13 building-block test projects in the 07 part 2.2 tree. |
| Consistency | 3 | 07 maps requirements to tests that document 20 does not use: `07:900` "REQ-DATA-001 ... TC-TST-124" against `20-traceability-matrix.md:772` "TC-DATA-951", and `07:903` "REQ-PERF-019 ... TC-TST-109, TC-TST-122" against `20:625` "TC-PERF-969". 05 contradicts its own open points. `05-service-catalog.md:224` says "Row 13 additionally needs the Appendix E change described in its cell", but `05:256` says "No binding: Saga 4 checks the lock with the `ConfirmYearResultsLocked` command". `05:129` says "attendance.* (Section 10 edge, no catalogued key yet)", but `05:257` says "The Attendance to Requests edge is closed by the `requests.saga-outcomes` queue". The home of `GrpcHopRules` differs: `07:806` "All in `tests/Architecture.Tests/Rules/`", and its 24 rules omit it, while `22-api-conventions-and-error-catalog.md:528` says "`GrpcHopRules` in `Nibras.BuildingBlocks.Testing`". `07:635` "rules R01 to R19 (nineteen rules, counted in kit-lint.mjs)" is wrong, because kit-lint has R01 to R34. | Use document 20's TC ids in the 07 Requirements covered table. Align 05:96/129/191/202/224 with open points 8 and 9. Add `GrpcHopRules` to 07 10.3 with a TC id and one location, and make 22 agree. Change "R19" to "R34". |
| Feasibility | 4 | The round-1 arithmetic defects are gone: `07:27` "Total: **206 .NET projects**", and `07:890` "71 + 3 × 25 + 50 = 196, inside the limit even when every allowance is spent at once". Unknowns now have owners: `05:257` "Which key carries the Attendance to Finance edge ... Architect". The one-hop rule is the plan's central synchronous constraint (`04:329` "The `GrpcHopRules` architecture test enforces it"), but the 07 structure gives it no project or test entry: `07:835` "Twenty-four rules." No document in the group states a team size or duration. It defers to 17 and 34, so buildability cannot be judged from these four documents alone. | Put `GrpcHopRules` in the 07 tree and in the 10.3 table, so an engineer knows where the one-hop test lives and which project it references. |
| Risk honesty | 4 | Contested requirements are now flagged, not stated as settled: `03:1109` "Three rows above read as settled while a product-owner decision or a logged conflict still holds them open". Each carries a default, owner, L×I score and RISK id (`03:1113-1115`, RISK-41, RISK-42, RISK-52). The 05 cycle classifications now have owners: `05:249` "Architect ... 2 \| 2 \| 4". Three residues remain. `07:957` "2026-09-20 \| planning session ... \| ready for Group B review \| none" is a stale self-verdict. `04:603` undercounts the pending ADRs. RISK-52's mitigation is due "Group B approval, before any metering slice" (`18-risk-register.md:184`), but 03 does not say that this approval depends on it. | Add to the 03 billing open point that RISK-52's ADR is due at Group B approval. Update the 07 review record. Make 04:603 count every Proposed ADR. |
| Testability | 3 | Every 03 row has an acceptance criterion (R21), and 07 10.3 names TC-TST-101 to 124. Several claims still point at the wrong proof or at none. `07:903` "No service references StackExchange.Redis directly" cites TC-TST-109, which asserts `07:818` "No `Nibras.BuildingBlocks.*` assembly references a `Nibras.<S>.*` assembly". `05:270` cites "the NetArchTest rule in document 07 that forbids any gRPC client reference not listed here", and no such rule is in 07 10.3. `07:950` "`/lint-plan` checks that every service in `05-service-catalog.md` has the folder, worker and contract project" contradicts ADR-0021:17 "it performs no other check". Unnamed reviews remain: `04:618` "Group B review diff against Section 9.1", and `04:623` "which documents 16 and 28 carry as identified test cases", with no TC id in 04 Section 10. | Add a TC-TST rule that bans `IConnectionMultiplexer` and StackExchange.Redis in service projects, and a TC-TST rule for gRPC client references against Section 8.0. Rewrite 07:934/950 as named review steps. Give each 04 Section 10 row a TC id. Name the reviewer and the recurrence in the 04:618-624 and 05:270/274/275 rows. |
| Distinctiveness | 3 | Structural documents, so the axis is scored on whether they carry the signature features traceably. That has not changed since round 1. 03 feature rows cite Appendix W as a source but give no rung, moment or demo proof: `03:547` "REQ-RPT-003 \| A morning brief per role ... Appendix W \| none \| TC-RPT-006". The rung appears only in isolated rows (`03:307` "at rung 2 degrading to the raw heatmap"; 7 mentions of "rung" in 1,132 lines). 04 still gives the ladder one paragraph (`04:560`). R34 now proves demo coverage at the Appendix W and O level, outside this group. | Add to 03 a table (or a column on signature rows) mapping each Appendix W feature number to its REQ ids, rung, autonomy level and Appendix O demo step and test. |
| Portability | 4 | 04 now names the non-Google fallback and the Huawei path: `04:63` "Devices without Google services \| the `IPushSender` fallback path", and `04:64` "Huawei push (AppGallery devices)". 07 still enforces portability on both runners (`07:43`). There are two new contradictions. `03:1103` REQ-PLAT-021 says "then the build fails naming the file", but `07:888` enforces the same rule by "code review and `.claude/rules/portability.md`". macOS is a supported developer platform (`04:344` "on Windows 11, Ubuntu 22.04 or later, or macOS 14 or later"), but `07:44` runs dev-smoke "on ubuntu-latest and windows-latest" only. | Name the portability analyzer in `Directory.Build.props` and cite it at 07:888, or re-word REQ-PLAT-021. Name the runner or manual check that proves macOS developer setup. |

**Verdict:** Blocked (blocking axes: Consistency, Testability, Distinctiveness)

| Round-1 gap | Now | Evidence |
|---|---|---|
| Synchronous-dependency graph misstated as a tree in 05, 04 and 07; contracts tree showed only school.proto | Closed | `05:225` "It is not a tree of depth one"; `04:322` "Platform, Scheduling, Academics and Hr are also called over gRPC"; `07:179` "all twenty contract projects carry `Grpc/<service>.proto`" |
| 07 Requirements covered: REQ ids with other meanings and TC ids 20 does not use | Partly closed | Meanings now quote 03 (`07:900` quotes `03:949` verbatim). The TC ids still differ from 20: `07:900` TC-TST-124 against `20:772` TC-DATA-951, and `07:903` TC-TST-109 against `20:625` TC-PERF-969 |
| REQ-ATT-017, REQ-INT-001, REQ-PLT-009 stated as settled | Closed | `03:1113-1115` give each a default, an owner, a score and a register id: "Product owner, Open Question 27 ... RISK-41", "Open Question 28 ... RISK-42", "RISK-52" |
| 03 lacks verification and open-points sections | Closed | `03:1107` "## Open points"; `03:1119` "## How this document is verified", with R19 to R23 named |
| Seven cycle classifications deferred to "Group B review" with no owner; 04:547 decisions without ADR | Closed | `05:245` "Each now has an owner and a default in force"; `05:249-255` scored rows; `04:600-601` "Architect, who records it as an ADR", with L×I. The ADRs are unwritten but honestly stated as pending |
| 07 part 2.3 event lists omit events 05 publishes | Closed | `07:172` "audit.integrity-check.failed, audit.retention.partition-detached"; `07:176` "ai.suggestion.rejected"; `07:157` "identity.break-glass.*" |
| 04/05 prove by "Group B review"; REQ mapped to TC-TST-109, which does not assert it | Partly closed | Some rows now name a reviewer (`04:617` "`plan-consistency-checker` maps every Section 3 edge"), but `04:618` "Group B review diff" and `05:274` "Group B review" remain. `07:903` still cites TC-TST-109 |
| Project count omits 13 test projects; path budget reaches 294 | Closed | `07:27` "Total: **206 .NET projects**"; `07:890` "71 + 3 × 25 + 50 = 196" |
| No signature feature in 03, 04 or 05 carries a rung or demo tag | Open | `03:547` "REQ-RPT-003 ... Appendix W \| none \| TC-RPT-006": no rung, moment or demo step. No such column or table exists in the group |
| 04 context table omits the non-Google push fallback and Huawei; 07 says kit-lint has R01 to R18 | Partly closed | `04:63-64` add both rows. `07:635` now reads "rules R01 to R19 (nineteen rules" while kit-lint has R01 to R34, so the count is still stale |

| Gap | Axis | Effort to close | Blocking |
|---|---|---|---|
| The 07 Requirements covered table cites TC ids that 20 does not use (TC-TST-124, 109, 122, 101-124 against TC-DATA-951, TC-PERF-969, TC-TST-951). REQ-PERF-019's claim "no StackExchange.Redis" is asserted by no test | Consistency | 2 hours | yes |
| 05 contradicts its own open points: 05:202/224 (Appendix E change for `assessment.grades.locked.v1`) against OP8 "No binding"; 05:96/129/191 (Attendance to Requests has no key) against OP9 and `11:476` `requests.saga-outcomes` | Consistency | 1 hour | yes |
| `GrpcHopRules`, the one-hop test that 04:329, 05:43 and 07:179 rely on, is missing from 07 10.3's "Twenty-four rules" and placed in the Testing block by 22:528. The gRPC-reference NetArchTest rule that 05:270 cites does not exist | Testability | 2 hours | yes |
| 07:934 and 07:950 claim `/lint-plan` checks per-service folders, workers and contract projects, and part 2.3 keys, which ADR-0021:17 and :37 say no rule does. The part 2.3 wildcards are not the backticked versioned keys R19 reads | Testability | 1 hour | yes |
| 04 Section 10 has no TC ids ("documents 16 and 28 carry"). 04:618-621, 623, 624 and 05:270, 274, 275 still prove by an unnamed "Group B review", with no recurrence after approval | Testability | 3 hours | yes |
| Signature features in 03 carry no rung, moment or demo-step and test mapping, so 32 and 34 cannot trace them from the catalog | Distinctiveness | 1 day | yes |
| REQ-PLAT-021 (`03:1103`, an analyzer fails the build) against `07:888` (code review only) | Portability | 1 hour | no |
| 04 Section 8 and 04:603 cover only ADR-0001 to 0017. ADR-0019 (Accepted) rewrote Section 8.0, which 04:328-330 depends on, and ADR-0018 and 0020 to 0023 are Proposed and unlisted | Completeness | 1 hour | no |
| 07:635 "R01 to R19 (nineteen rules)" against 34 rules; the 07:957 review record is stale; 05:18 repeats the universal Platform calls that 05:41 says are not repeated | Consistency | 30 minutes | no |
| macOS developer support (04:344, REQ-PLAT-001) has no runner: dev-smoke runs on ubuntu-latest and windows-latest only (07:44) | Portability | 1 hour | no |
