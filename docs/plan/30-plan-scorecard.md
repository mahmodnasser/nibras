# 30. Plan Scorecard

## Purpose

This document records how good the plan is, group by group, against the seven-axis rubric in `PLAN_SPEC.md` ("The scorecard"), with quoted evidence for every score. **A group is approved only at 4 or better on every axis; an average never substitutes for a low score.** It exists so the product owner approves the plan on evidence, and so the remaining work before approval is a named, sized list rather than a feeling.

## Scope

| In scope | Owned elsewhere |
|---|---|
| Scores for Groups A to F with evidence, the verdict per group, the gaps and the shortest path to approval | The rubric: `PLAN_SPEC.md`; the procedure: `/score-plan` and `docs/templates/scorecard.md` |
| The consolidated remediation plan across groups | The brief corrections themselves: ADR-0019 and the v9.1 briefs |
| | Document 30 does not score itself, and document 00 is scored after it is written |

## Content

### 1. Method

Each group was scored by an independent reviewer instructed to be adversarial, to quote the document with file and line for every score, and to reserve 5 for work that needs no change. Every reviewer was given the same established facts: `kit-lint` is clean including rule R19 (every cited identifier exists in its catalog); document 03 holds 860 requirements; document 34 builds 858 of them in 718 slices and explains the other 2; document 20 gives every requirement a test identifier. The reviewers were then asked to find what tooling cannot: contradictions of meaning, thin sections, unowned unknowns, claims with no test. The four most consequential findings were re-checked by hand before this document was written (Section 4).

### 2. Summary

| Group | Completeness | Consistency | Feasibility | Risk honesty | Testability | Distinctiveness | Portability | Lowest | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| A | 3 | 2 | 3 | 3 | 2 | 3 | 4 | 2 | Blocked (6 axes) |
| B | 3 | 2 | 3 | 3 | 3 | 3 | 4 | 2 | Blocked (6 axes) |
| C | 4 | 3 | 4 | 3 | 4 | 3 | 3 | 3 | Blocked (4 axes) |
| D | 4 | 2 | 3 | 3 | 3 | 4 | 4 | 2 | Blocked (4 axes) |
| E | 3 | 2 | 2 | 3 | 3 | 3 | 4 | 2 | Blocked (6 axes) |
| F | 3 | 2 | 3 | 3 | 3 | 3 | 4 | 2 | Blocked (6 axes) |
| **Lowest per axis** | **3** | **2** | **2** | **3** | **2** | **3** | **3** | | |

**Verdict: no group is approved yet.** Portability is the one axis at 4 or better almost everywhere. The plan is complete in coverage (every requirement is specified, built by a slice and tested) but not yet consistent in meaning, and its schedule does not match its own work breakdown. Every gap below is sized; none requires rethinking the architecture.

### 3. Scores by group

#### 3.1 Group A: Understanding (01, 02; 00 last)

| Axis | Score | Evidence | What would raise it by one |
|---|---|---|---|
| Completeness | 3 | Document 00 is not written; PLAN_SPEC orders it last (`PLAN_SPEC.md:117` "then update 00"), so the group is incomplete by design, and score-plan says to score it that way (`score-plan.md:43` "score the group as incomplete"). Document 01 claims full coverage (`01-questions-and-assumptions.md:3` "Every open point in `docs/project/OPEN_QUESTIONS.md` restated"), but the two pending product-owner decisions are missing from both files: absence-alert timing (`brief-findings.md:43` "Needs the product owner") and the Tier 1 API (`02-competitive-gap-analysis.md:135` "Product owner, as an ADR"). Neither document has an Open points section. Document 02 does have a recommendation for every gap (`02:84-97`). | Add questions 27 (absence alert: 30 minutes or 30 seconds) and 28 (public API, OneRoster and iCal at Tier 1) to OPEN_QUESTIONS and to document 01, each with a default, an impact, the phases it moves and an owner. Add an Open points section to 01 and 02. Write 00 after Group F. |
| Consistency | 2 | The matrix contradicts the text. The API row has six "yes" cells (`02:44`), but the text says `02:69` "Seven of ten products publish an application programming interface" (repeated at 96, 119, 135, and copied into `32-product-differentiation-and-demo.md:97`). Phase error: `02:143` "Phases in master brief Section 28 already put attendance, localization, safety and platform in Phase 1", but `01-master-brief.md:1198` puts Attendance in Phase 2. The count at `01:135` does not add up: "Five are architect decisions with a Proposed ADR (1, 7, 8, and the two halves of 11". Its own table gives the .NET owner as `01:66` "Product owner", and item 11 has no ADR. Question 17 drifts: `01:31` says "Huawei push adapter moves from Tier 2 to Phase 2", while `OPEN_QUESTIONS.md:25` says "Tier 2 or Tier 3". | Correct "seven" to "six, plus two partial" in 02 and 32. Fix `02:143` to say Attendance is in Phase 2. Recount the section 8 summary (3 architect items with an ADR, 9 product-owner defaults, 1 settled) and give .NET one owner. Align the question 17 impact with OPEN_QUESTIONS. |
| Feasibility | 3 | Impacts are given without numbers: `01:42` "Each is a full service sheet, so the cost is measured in weeks, not days". Question 24 names no durations, only `01:53` "scale roughly in proportion to backend headcount". The pre-GA re-check is owned by a tool, not a person, and has no effort: `02:142` "Owner: domain expert agent; timing: before the general-availability re-check". An agent cannot run a vendor trial or a sales conversation. 72% of the evidence base is unverified (`02:55` "123 are **unverified**"). | Size each "Impact if wrong" in slice-days, taken from document 34 capacity where a question moves a capability. Give the competitor re-check a human owner, a date relative to Phase 6, and an effort estimate. |
| Risk honesty | 3 | Document 01 has no likelihood column (`01:13` header: "Impact if wrong \| Plan documents affected \| Phases affected \| Evidence... \| Owner"). Each owner is simply "Product owner", whose silence accepts the default (`01:7`), so no one owns mitigation. Document 02 is stale against document 18: `02:137` "Add to `18-risk-register.md` when written", although `18-risk-register.md:173` already carries RISK-31. On the other side, document 02 is candid about unverified absences (`02:7` "silence is recorded as unverified rather than as 'no'"). | Add likelihood and a mitigation to every default in 01, and link each default to its RISK id in 18. Replace "when written" in 02 with RISK-31. |
| Testability | 2 | Document 01's main proof is a check that does not exist: `01:143` "`/lint-plan` compares the two files by question number". The only kit-lint rule that mentions OPEN_QUESTIONS is R05 (`tools/kit-lint/kit-lint.mjs:235-236`). Document 02's proof ids do not test what it claims. Webhooks are "proved" by TC-INT-001 (`02:45`), but `23-integrations-and-public-api.md:750` defines TC-INT-001 as asserting `GATEWAY_PERMISSION_DENIED` on untagged operations. TC-PLT-003 at `02:40` means smart defaults, while `12-security-privacy-safety.md:632` uses it for the owner invitation, a collision recorded at `06-services/platform.md:2230`. | Add a kit-lint rule that diffs question numbers and defaults between 01 and OPEN_QUESTIONS, or change the claim to a manual review with a sign-off row. Give webhook delivery its own test id in document 20. Resolve the TC-PLT-002 to 005 collision and cite the id that survives. |
| Distinctiveness | 3 | Document 02 sharpens claims by rewording them against evidence (`02:65` "**Reworded.** 'An Arabic interface' is table stakes"). But the list of "unmatched" features is miscounted. `02:55` says "The five capabilities on which no competitor scored yes or partial", yet the offline row (`02:43`) also has no yes or partial, so there are six and the signature feature for offline mobile is left out. Three "unmatched" claims rest on marketing silence (`02:67` "Unmatched on the check date"). The document names no moment or rung for any signature feature; it defers to document 32. | Correct the count to six and state which are signature features. For features 27, 29, 31 and 32, add the Appendix O minute and the Appendix W rung beside the test id. Mark every "unmatched" claim as not speakable to a buyer until the trial resolves it. |
| Portability | 4 | Document 01 names the platform assumptions and their fallbacks: `01:55` "served by the Linux virtual machine appliance for Hyper-V or VMware"; `01:110` "The `dev-smoke` job runs one Podman and one Docker configuration"; `01:54` iOS through "hosted macOS runner minutes"; `01:31` devices without Google services, with a ten percent trigger. Gaps: no runner is named for the no-Google Android build, and document 02 compares no competitor on mobile platforms or on devices without Google services. | Name the CI runner or device lab that proves the no-Google build and the AppGallery path. Add a matrix row to 02 for mobile platform coverage, including devices without Google services. |

**Verdict:** Blocked (blocking axes: Completeness, Consistency, Feasibility, Risk honesty, Testability, Distinctiveness)

| Gap | Axis | Effort to close | Blocking |
|---|---|---|---|
| The API count ("seven of ten") contradicts the matrix (six yes); it is repeated five times in 02 and copied into 32, and it underpins the Tier 1 API recommendation | Consistency | 1 hour | yes |
| `02:143` puts Attendance in Phase 1, against master brief Section 28 (Phase 2) | Consistency | 0.5 hour | yes |
| Two pending product-owner decisions (absence-alert timing; public API, OneRoster and iCal at Tier 1) are missing from 01 and OPEN_QUESTIONS, although document 34 already builds OneRoster in Phase 3 | Completeness | 3 hours | yes |
| `01:143` claims a lint comparison with OPEN_QUESTIONS that kit-lint does not implement | Testability | 4 hours (add a rule) or 0.5 hour (reword) | yes |
| TC-INT-001 cited as the proof for webhooks, but it tests refusal of untagged operations; TC-PLT-003 collides across Appendices R and W | Testability | 3 hours | yes |
| The section 8 summary in 01 misclassifies items (5 architect / 7 product owner) and conflicts with the .NET owner | Consistency | 1 hour | yes |
| No likelihood or mitigation per default in 01; no link to RISK ids; "when written" in 02 is stale against RISK-31 | Risk honesty | 4 hours | yes |
| The count of "unmatched" capabilities (five) leaves out offline mobile attendance; no moment or rung is given per signature feature | Distinctiveness | 2 hours | yes |
| The competitor re-check is owned by an agent, with no effort or date; impacts are not sized in slice-days | Feasibility | 4 hours | yes |
| Document 00 is not written (by design, after Group F) | Completeness | 1 day | no |

#### 3.2 Group B: Requirements and architecture (03, 04, 05, 07)

| Axis | Score | Evidence | What would raise it by one |
|---|---|---|---|
| Completeness | 3 | 03 has neither a "How this document is verified" section nor an "Open points" section, although PLAN_SPEC.md:128 requires the first ("Every document ends with 'How this document is verified'"). 03 ends with the table row at `03-requirements-catalog.md:1105` ("REQ-PLAT-023 ... Kit archives are built with a tool..."). 04 leaves decisions without ADRs: `04-architecture-overview.md:547` "Decisions this document relies on that have no ADR yet, and which Group B must either confirm". 05 limits events to 3 to 5 per service, `05-service-catalog.md:7`: "list the three to five events that define the service's place". 07 is the most complete: its trees, rules and template are all present. | Add a verification section and an open-points section to 03. Write the ADRs that 04:547 lists (the job reply channel exemption, the non-Requests orchestrator sagas, and the phase placement of Ai). |
| Consistency | 2 | 05 says the synchronous graph is a tree, `05-service-catalog.md:219`: "Every gRPC dependency in the catalog points at School or Identity, neither of which calls anything". The sheets say otherwise: `06-services/identity.md:604` "Platform `nibras.platform.v1.Settings.GetSettings`", and `06-services/attendance.md:432` "Consumed \| `nibras.scheduling.v1` `Timetables`". 07's requirement ids mean something else in 03: `07-solution-structure.md:891` "REQ-PLAT-001 \| Twenty services, sixteen Tier 1" against `03-requirements-catalog.md:1083` "REQ-PLAT-001 \| Development is supported on Windows 11". Likewise `07:894` "REQ-PERF-002 \| Caching goes through" against `03:750` "REQ-PERF-002 \| An API read from the database is p95 under 250 ms". Document 20 maps these ids to TC-PLAT-951 and TC-PERF-952 (`20-traceability-matrix.md:886,608`), not to the TC-TST ids in 07. The contracts tree drops events that 05 lists as published: `07:172` "audit.integrity-check.failed" has no `audit.retention.partition-detached`, which `05:33` lists, and `07:176` "ai.usage.recorded, ai.index.rebuild-completed" has no `ai.suggestion.rejected`, which `05:37` lists. The same gap appears for Behavior, Wellbeing and Notification. `07:626` says "rules R01 to R18", but kit-lint has R19. | Rewrite the synchronous-dependency column in 05, 04:323 and 07:461/504 from the sheets, and add every exposed proto to the 07 contracts tree. Fix the 5 requirement ids in the 07 "Requirements covered" table. Make the 07 part 2.3 event lists match the 05 Publishes column. Change R18 to R19. |
| Feasibility | 3 | The 07 project count leaves out projects it creates itself. `07-solution-structure.md:27` "Total: **193 .NET projects**" omits the 13 building-block test projects that `07:148` creates ("a test project `tests/Nibras.BuildingBlocks.<Name>.Tests/` beside it"). The 07 path budget can break its own limit, `07:881`: "70 + 4 × 41 + 60 = 294 only if every limit is hit at once", against a 200-character rule. 05 says Attendance to Finance and Requests have no catalogued key (`05:90`), and the Hr, Operations and Identity cycles are left to "Confirm at the Group B review" (`05:189`). No document in the group states a team size or a duration against which buildability could be judged. | Correct the project count. Set the per-project path budget so that the worst case is at most 200. Close the two Attendance edges in 05 instead of deferring them to document 11. |
| Risk honesty | 3 | The 07 open points have owners and impact: `07-solution-structure.md:930` "Architect \| Error codes are declared twice and TC-TST-101 is rewritten". 05 defers 7 cycle classifications to a review, with no owner or default: `05-service-catalog.md:189` "Confirm at the Group B review". 03 states the absence-alert timing as settled while the product-owner decision is pending: `03-requirements-catalog.md:361` "enqueued within 30 seconds of the mark \| 1 \| ... \| WF-ATT-01". WF-ATT-01 says 30 minutes. 03 also puts the public API in Tier 1 while that decision is pending: `03:925` "open by default through a public REST API, outgoing webhooks, iCal feeds \| 1". 03 cites the rule that brief-findings says it conflicts with: `03:131` "REQ-PLT-009 ... \| BR-FIN-017". About 11 brief-findings defects touch this group, and none is flagged in 03, 04 or 05. 04 and 05 contain no risk with likelihood or owner. | Mark REQ-ATT-017, REQ-INT-001 and REQ-PLT-009 as pending, each with an owner, a default and a link to OPEN_QUESTIONS. Give each "Confirm at the Group B review" row an owner and a default. Add an open-points table to 04 and 05 listing the brief-findings items that touch them. |
| Testability | 3 | Every 03 row has an acceptance criterion: `03-requirements-catalog.md:65` "A real test case identifier ... otherwise a one-line Given, When, Then with a concrete number". 04 proves its quality attributes with tools, not test-case ids: `04-architecture-overview.md:602` "which documents 16 and 28 carry as identified test cases". 05 cites no test case at all. It proves its claims by review, `05-service-catalog.md:243`: "Group B review diff". 07 maps a claim to the wrong test: `07-solution-structure.md:894` "No service references StackExchange.Redis directly ... \| TC-TST-109, TC-TST-122". TC-TST-109 checks that building blocks do not reference services (`07:809`), so no test covers "no direct StackExchange.Redis". | Add a TC-TST rule that bans direct StackExchange.Redis use in services. Replace each "Group B review" proof in 04 and 05 with a TC id or a named lint rule, such as a gRPC-reference architecture test and a cycle-check lint id. |
| Distinctiveness | 3 | These are structural documents, so the axis applies mainly to whether they carry the signature features faithfully. 04 gives the assist ladder a single dense paragraph: `04-architecture-overview.md:553` "The product is complete and sellable at rung 1; a feature above it falls back downward". 03 has feature rows such as `03-requirements-catalog.md:555` "Every automated action shows a Because panel with its reasons ... \| TC-RPT-008". No document in the group links a signature feature to its moment, its rung or a sixty-second proof, and 05 gives the Ai row no rung per feature (`05:37` "rung 1 and 2 features ship inside their owning services earlier"). | In 03, tag each signature-feature row with its Appendix W rung and its Appendix O demo minute, or a reserve-bank reference, so that 32 and 34 can trace them. |
| Portability | 4 | 07 enforces portability instead of recommending it: `07-solution-structure.md:43` "kit-lint, its own tests, and the service-template smoke test, on ubuntu-latest and windows-latest". 07 also sets culture and time-zone runners: `07:523` "the reminder cut-off across Riyadh, Amman and Dubai". 03 covers devices without Google services: `03-requirements-catalog.md:476` "On a device without Google services, push arrives in-app while the app is open". The 04 external-systems table names only FCM and APNs: `04-architecture-overview.md:62` "Firebase Cloud Messaging, Apple Push Notification service". It has no row for the non-Google fallback or for the Huawei adapter (REQ-MOB-039). | Add the fallback for devices without Google services, and the Tier 2 Huawei adapter, to the 04 context table. Name the runner that proves each platform-specific row in 03 PLAT. |

**Verdict:** Blocked (blocking axes: Completeness, Consistency, Feasibility, Risk honesty, Testability, Distinctiveness)

| Gap | Axis | Effort to close | Blocking |
|---|---|---|---|
| Synchronous-dependency graph: 05:219, 04:323 and 07:461/504 claim only School and Identity are called, but the sheets call Platform, Scheduling, Identity, Hr, Academics and Ai over gRPC. The contracts tree shows only school.proto. | Consistency | 1 day | yes |
| The 07 "Requirements covered" table uses REQ-PLAT-001, REQ-PLAT-003, REQ-PLAT-004 and REQ-PERF-002 with meanings that differ from 03 and 20, and maps them to TC ids that 20 does not use. | Consistency | 2 hours | yes |
| REQ-ATT-017 (30 s), REQ-INT-001 (public API in Tier 1) and REQ-PLT-009 (conflicts with BR-FIN-017) are stated as settled while a product-owner decision or a logged defect is still open. | Risk honesty | 3 hours | yes |
| 03 has no "How this document is verified" section and no open-points section. | Completeness | 3 hours | yes |
| 7 cycle classifications in 05 are deferred to "Confirm at the Group B review" with no owner or default. 04:547 lists decisions that have no ADR. | Risk honesty | 1 day | yes |
| The 07 part 2.3 contract event lists leave out events that 05 lists as published (Audit, Ai, Behavior, Wellbeing, Notification, Identity join/break-glass). | Consistency | 3 hours | yes |
| 04 and 05 prove their claims by "Group B review" rather than by a test-case id. REQ-PERF-002 in 07 is mapped to a test (TC-TST-109) that does not assert it. | Testability | 1 day | yes |
| The count of 193 projects omits the 13 building-block test projects. The path budget allows 294 characters against a limit of 200. | Feasibility | 2 hours | yes |
| No signature feature in 03, 04 or 05 carries a rung and a demo-minute tag. | Distinctiveness | 1 day | yes |
| The 04 context table omits the push fallback for devices without Google services and the Huawei adapter. 07 says kit-lint has rules "R01 to R18", but it has R19. | Portability | 1 hour | no |

#### 3.3 Group C: Services, data, messaging, performance (06, 10, 11, 21)

| Axis | Score | Evidence | What would raise it by one |
|---|---|---|---|
| Completeness | 4 | All 23 sheets carry sections 1 to 16 plus Open points and "How this document is verified" (grep of H2 headings). But 11 data-owning sheets have no `stateDiagram`, including all three saga orchestrators: `06-services/platform.md:658` "The saga designs, steps, compensations, timeouts ... are `13-workflows-and-sagas.md` Saga 1, Saga 2 and Saga 10". The same applies to School (Sagas 4 and 5) and Admissions (Saga 3). PLAN_SPEC:104 requires "sagas with state diagrams and compensations" in the sheet. Numbering drifts: `identity.md:28`, `platform.md:28`, `audit.md:28` and `gateway.md:27` open at "## 2. Responsibilities" with no section 1 | Add a state diagram (or an embedded copy of doc 13's) to the Platform, School and Admissions sheets and to the other workflow-owning sheets that lack one. Normalise section numbering across all 23 sheets |
| Consistency | 3 | Lint is clean, but R19 does not catch contradictions between the plan documents themselves. `21-performance-engineering.md:984` says "`CREATE INDEX ix_embeddings_hnsw ON ai.embeddings`", and `10-data-architecture.md:266` says "`embeddings` \| Ai \| Hash by `tenant_id`, 8 partitions". Both contradict `06-services/ai.md:100` "`IndexedChunk` (`ai_index.embedding_chunk`)". `21-performance-engineering.md:716` says "Money columns are numeric(19,4)", against `10-data-architecture.md:237` "`numeric(18,4)`". `finance.md:1584` claims "Resolved 2026-09-22: documents 10 and 21 now describe the row lock", yet `21-performance-engineering.md:721` still has "`ux_series_code ON finance.series (tenant_id, code)`" and not the sheet's campus-keyed index. About 34 of the 49 bullets in brief-findings.md touch this group | Align docs 10 and 21 to `ai_index.embedding_chunk` and its scope columns, to `numeric(18,4)`, and to `ux_series_campus_code`. Remove the false "Resolved" note. Add a lint rule for table and index names across 10, 21 and 06 |
| Feasibility | 4 | Most unknowns have a named owner and default, e.g. `identity.md` Open point 13 "Add the prefix with Identity as writer and the Gateway as reader". But the leaver rule the Gateway depends on cannot be built as doc 21 stands: `21-performance-engineering.md:390` "`svc_gateway` ... `~nibras:platform:platform:tenant-by-host:*` read-only; `~state:ratelimit:*:edge:*`". Doc 21's Open points (lines 1271-1278) do not record this gap. `11-messaging-architecture.md` Open point 5 leaves dispatcher throughput at peak unmeasured until phase 6 | Update doc 21 §2.2/§2.3 with the `state:revoked:` prefix and the Gateway ACL. Carry every sheet-raised open point against docs 10, 11 and 21 into those documents' own Open points, each with a due phase |
| Risk honesty | 3 | The sampled sheets have full risk tables, e.g. `finance.md:1535` "A gap or duplicate in a series ... \| low \| high: a regulator rejects the series ... \| Finance lead". But docs 10, 11 and 21 contain no risk table: "likelihood" appears 0 times in each. Their Open points carry impact only, e.g. `10-data-architecture.md:626` "A hot partition at 20,000 students may need the split before general availability". The Wellbeing sheet defaults to putting level-S answers on a device (`wellbeing.md:1053` "may wait in the encrypted outbox until sync"), and its risk table (lines 1001-1010) has no row for this | Add a risk table with likelihood, impact, mitigation and owner to docs 10 (restore, RLS bypass, partition hot spots), 11 (message loss, lane starvation, dispatcher leader) and 21 (budget breach, cache stampede, pool saturation). Add the device-outbox risk to Wellbeing |
| Testability | 4 | The sheets map claims to ids densely (28 to 84 distinct TC ids per sheet), e.g. `identity.md:2092` "TC-SEC-040, TC-SEC-047 \| ... revocation effective within 5 s on web and mobile". Doc 11 has 1 distinct TC id in its whole testing section. Its guarantees are named classes only, e.g. `11-messaging-architecture.md:1041` "`LaneIsolationTests.BulkBacklog_UrgentStillWithin30Seconds`". TC-ATT-003 (`attendance.md:1116`, "alert enqueued within 30 s") asserts a timing that is still an open product-owner decision | Give every doc 11 suite (chaos, deliver-twice, ordering, fairness, lanes, pacts) a TC id registered in doc 16/20. Mark TC-ATT-003 as conditional on the absence-alert decision |
| Distinctiveness | 3 | These documents barely touch signature features, but the mechanisms that serve them are sharp: tenant fairness windows (`11-messaging-architecture.md:691` "Admission window \| The producer of a bulk job"), pre-peak warm-up, gate-pass codes and level-S isolation. No sheet or architecture document ties a mechanism to a signature feature's moment, rung or sixty-second proof: "signature feature" and "Appendix O" occur 0 times in docs 10, 11, 21 and in 19 of 23 sheets. Brief-findings line 63 notes that feature 39, calendar-aware scaling (doc 21 §9), "cannot be shown on demo data" | Add a "Signature features served" table per sheet and to docs 11 and 21, naming the feature, its Appendix O minute and the TC that proves it. Resolve the feature-39 demotion |
| Portability | 3 | "Linux" appears 0 times in all 23 sheets and in docs 10 and 21. Only doc 11 names runners: `11-messaging-architecture.md:1027` "on Windows and Linux runners". Devices without Google services are covered only in Notification: `notification.md:818` "A device without Google services receives push in-app while open; urgent falls back to SMS". RTL is absent from 11 sheets that own bilingual `LocalizedText` or search, e.g. Attendance and Finance (0 RTL mentions) | Add a portability row to each sheet's test plan: culture-invariant parsing, Arabic collation and search, RTL rendering of generated documents, and the runner (Windows or Linux) that proves each. Add the same to docs 10 and 21 for ICU collation and time zones |

**Verdict:** Blocked (blocking axes: Consistency, Risk honesty, Distinctiveness, Portability)

| Gap | Axis | Effort to close | Blocking |
|---|---|---|---|
| Ai index named `embeddings`/`scope_code` with 8 hash partitions in docs 10 and 21, but `ai_index.embedding_chunk` with permission and campus scope columns in doc 25 and the Ai sheet. The Ai sheet calls doc 21 "binding" but overrides it | Consistency | 3 hours | yes |
| Doc 21 §2.3 Redis ACL lacks the `state:revoked:` prefix and the Gateway reads (key set, maintenance, security settings), so WF-IDN-06's leaver refusal cannot be implemented as written | Feasibility | 2 hours | yes |
| The Finance sheet marks its Open point 1 "Resolved", but doc 21 still uses `ux_series_code (tenant_id, code)` and `numeric(19,4)` against doc 10's `numeric(18,4)` | Consistency | 1 hour | yes |
| No risk register (likelihood, impact, owner, mitigation) in docs 10, 11 or 21 | Risk honesty | 1 day | yes |
| No sheet or architecture document links mechanisms to signature features, Appendix O minutes and proofs; the feature-39 demo gap is open | Distinctiveness | 1.5 days | yes |
| Sheets never name the runner, OS or RTL behaviour they depend on (Arabic search, culture, generated-document direction); no-Google coverage exists only in Notification | Portability | 2 days | yes |
| About 34 open brief-findings touch Group C (Appendix E missing events and bindings, Appendix B/K/C gaps, RA 8.0 sync dependencies added by 7+ sheets without an ADR). Sheets publish workarounds pending ADRs with no deadline | Consistency | 3 days (ADR plus a v9.1 brief bump) | yes |
| Saga orchestrator sheets (Platform, School, Admissions) and 8 other workflow owners have no state diagram and defer to doc 13 | Completeness | 1 day | no |
| Doc 11 messaging guarantees carry class names but no TC ids; TC-ATT-003's 30 s assertion depends on the pending absence-alert decision | Testability | 4 hours | no |
| The Wellbeing default stores level-S check-in answers in the device outbox, against the "never reaches a device" rule, and has no risk row; the decision rests with the privacy officer | Risk honesty | 2 hours plus a product decision | no |

#### 3.4 Group D: Web, mobile, security, workflows, design (08, 09, 12, 13, 14)

| Axis | Score | Evidence | What would raise it by one |
|---|---|---|---|
| Completeness | 4 | Every PLAN_SPEC row for 08, 09, 12, 13 and 14 is present and deep: 191 screen rows (`08-web-structure.md:793`), Drift schema, 10 sagas, 21 threat tables. Thin spots: the required permission matrix is deferred, `12-security-privacy-safety.md:545` "are defined in **Appendix I** and are not restated here". `14-design-system-and-ux.md:507` says "Seventeen screens across every workspace", but §9.1 to §9.17 have no HR screen. 08, 09, 12 and 14 have no Open points section; only 13 has one (`13-workflows-and-sagas.md:810`). | Add an HR key screen to 14 §9. Add a derived per-template permission summary to 12 §5 (or an explicit PLAN_SPEC waiver). Add Open points sections to 08, 09, 12 and 14. |
| Consistency | 2 | The live permission refresh has three different fan-outs. `08-web-structure.md:460` says "Communication owns the SignalR hubs … It consumes the event" on queue `communication.permission-refresh` (`:447`). `11-messaging-architecture.md:436` routes it on `communication.tenant-lifecycle`. `12-security-privacy-safety.md:413` has "MQ-->>Web: consumer pushes SignalR refresh". `12-security-privacy-safety.md:439` "WEL->>NOT: identity.break-glass.used.v1" contradicts `06-services/wellbeing.md:1058` "Wellbeing never publishes on `nibras.identity`". The cooling-off period differs: `13-workflows-and-sagas.md:794` "Appendix R value of 7 days" against `12-security-privacy-safety.md:679` "30-day cooling-off". The same test ids mean different things: `13-workflows-and-sagas.md:179` uses TC-PLT-005 for "Step 3 fails", but `08-web-structure.md:635` uses it for "Configuration as code". `13-workflows-and-sagas.md:124` sends `notification.notification.requested.v1` as the command, which `11-messaging-architecture.md:290` forbids. `08-web-structure.md:793` "Every row uses only components from Section 6", yet rows use `register-card`, `exception-bar` and `filters`. `14-design-system-and-ux.md:129,271` cite "§13", which does not exist. | Pick one owner and one queue for the permission refresh, and align 08, 11 and 12. Make Wellbeing alert through `RequestNotification`. Align the cooling-off period in 12 §10.2 and §10.3 with 13, or log the conflict. Renumber the TC-PLT saga ids. Replace event-as-command rows in 13 with `RequestNotification`. Fix the 08 §7 claim and the dangling §13 references in 14. |
| Feasibility | 3 | Several proofs depend on tooling that does not exist: `12-security-privacy-safety.md:810` "`tools/threat-model-lint`: parses every STRIDE and abuse table". `14-design-system-and-ux.md:817` "`tools/design-parity` compares the three lists". `tools/` contains only dev-setup, kit-lint, license-scan and plan-build. Pending product-owner decisions are not owned in these documents. `09-mobile-structure.md:604` puts `attendance.student.absent.v1` in the Urgent category, which "breaks quiet hours", while the 30 min vs 30 s timing is undecided. 13's Open points (`:814-815`) omit it. `09-mobile-structure.md:721` "Store review time \| Assumed up to three days" leaves Apple build capacity unowned. | Add the pending decisions (absence-alert timing, Apple build and account capacity, target countries and certifications) to the Open points of 09, 12 and 13, each with a default and an owner. Specify the three promised tools as work items with slices, or replace them with kit-lint rules that exist. |
| Risk honesty | 3 | `12-security-privacy-safety.md:76` "Likelihood and impact are `low`, `med`, `high`": the threat tables have no owner and no residual-risk column, and no link to `18-risk-register.md`. `09-mobile-structure.md:552` redefines a failing test so it passes: "`TC-MOB-102` (queued approvals apply once on reconnect) is satisfied by the pending state of the *nudge*". Eight entries in `tools/plan-build/parts/brief-findings.md` touch Group D (U vs M, R permission names, retention job names, missing E bindings for 13, R keys absent from E, absence-alert timing, TC-PLT collisions, cooling-off and invitation expiry). None has an owner inside 08, 09, 12 or 14. Credit: `09-mobile-structure.md:283` states the Appendix U/I vs M conflict openly. | Add an owner and mitigation-status column to every threat of impact high or critical in 12 §2, and link critical rows to 18. Mark TC-MOB-102 as blocked on a brief fix instead of redefining it. List each brief-findings item that touches the document under its Open points. |
| Testability | 3 | `12-security-privacy-safety.md:9` says ids "minted here … become the security and privacy test plan that `16-test-strategy.md` schedules". 101 of the 250 TC ids in 12 appear in none of 16, 20 or 06 (for example TC-SEC-182 and TC-SEC-183, both on high-impact threats). The same gap: 14 ids in 09, 11 in 08, 5 in 13 and 3 in 14. So the lint that 12 proposes would fail on 12 itself (`:810` "names an identifier absent from `20-traceability-matrix.md`"). `13-workflows-and-sagas.md:720` "identifiers are assigned in `16-test-strategy.md` under the `DATA` area": 16 has none for tier migration. `09-mobile-structure.md:959` says "`TC-MOB-701` to `TC-MOB-710` in §3.9 map one to one", but §3.9 (`:585-592`) omits 704 and 707. | Register every locally minted TC id in 16 and 20 with its owning suite. Assign ids to the Saga 10 scenarios. Complete the §3.9 table. Resolve the TC-PLT-002 to 005 collision. |
| Distinctiveness | 4 | The signature patterns are specified to build level. `14-design-system-and-ux.md:388` "A screen earns at most one delight moment". `14-design-system-and-ux.md:493` Because panel "refuses to render a payload whose rung is 2 or above with no factors". The theming algorithm has worked contrast values (`14-design-system-and-ux.md:288-293`). The offline banner rule is at `09-mobile-structure.md:550`. The gap: only a few screens tie a signature feature to a sixty-second proof, e.g. `14-design-system-and-ux.md:501` "the Appendix O minute 5 step and `TC-RPT-008`". Most key screens in §9 name no Appendix O minute or rung. | Give each 14 §9 key screen and each signature row in 08 §7 its Appendix W rung and Appendix O minute, or mark it reserve-bank per the document 32 finding. |
| Portability | 4 | Desktop kiosk on both operating systems: `09-mobile-structure.md:699` "Windows and Linux desktop builds take no `--flavor`". No-Google path: `09-mobile-structure.md:635` detects missing Play Services at runtime and links no Firebase in that variant. iOS limits: `09-mobile-structure.md:648` "No screen ever says 'syncing in the background'". RTL rules for icons, charts, grids and seating (`14-design-system-and-ux.md:319-360`). Defect: `09-mobile-structure.md:948` "Every capability a parent or student has on the web is `full` on the mobile app" contradicts `:883` "Quiz player and question bank \| full \| none". Huawei is deferred (`09-mobile-structure.md:640`). | State the quiz-player exception to Section 18, with its reason and a mobile-web fallback. Give the Huawei/HarmonyOS deferral an owner and a trigger threshold. Name the runner that proves the iOS white-label build per school. |

**Verdict:** Blocked (blocking axes: Consistency, Feasibility, Risk honesty, Testability)

| Gap | Axis | Effort to close | Blocking |
|---|---|---|---|
| Cross-document contradictions: the permission-refresh owner and queue (08:447/460, 11:436, 12:413); Wellbeing publishing `identity.break-glass.used.v1` (12:439); sagas sending `notification.notification.requested.v1` as a command (13 vs 11:290); cooling-off 7 vs 30 days (13:794 vs 12:679,697) | Consistency | 1 day | yes |
| 134 locally minted TC ids (101 in 12, 14 in 09, 11 in 08, 5 in 13, 3 in 14) are absent from 16 and 20. Saga 10 has no ids. TC-PLT-004/005 carry two meanings (13:178-181 vs 08:597,635) | Testability | 2 days | yes |
| Pending product-owner decisions are unowned in these documents: absence-alert timing (09:604, 13 WF-ATT-01), Apple build and account capacity (09 §5), target countries and certifications (12 §10 has no country law or certification) | Feasibility | 4 hours | yes |
| Threat tables in 12 §2 have no owner or residual-risk column and no link to 18. TC-MOB-102 is redefined to pass (09:552). The 8 brief-findings entries are not listed under any Open points | Risk honesty | 1 day | yes |
| Proofs rely on tools that do not exist: `tools/threat-model-lint` (12:810), `tools/design-parity` (14:817), a "/lint-plan" parity check (09:969) | Feasibility | 1 day | yes |
| 08, 09, 12 and 14 have no Open points section with default, owner and impact, as PLAN_SPEC requires | Completeness | 4 hours | no |
| Component inventory drift: 08:793 claims only Section 6 components are used; the mobile-only widgets `conflict_banner`, `pending_row_badge` and `sync_failure_banner` (09 §3.7) are absent from 14 §7, so the parity check in 14:817 would fail | Consistency | 4 hours | no |
| 14 cites a nonexistent §13 (129, 271). Dark status ratios are measured on `#1B1B1F` (14:141), not the dark `surface` `#0C1014` (14:138) | Consistency | 2 hours | no |
| No HR key screen in 14 §9. The 12 §5 role matrix is deferred to Appendix I | Completeness | 4 hours | no |
| Quiz player is web-only against the "students do everything on mobile" claim (09:883 vs 09:948) | Portability | 2 hours | no |

#### 3.5 Group E: Operations, testing, roadmap, risk, dependencies, traceability (15 to 20)

| Axis | Score | Evidence | What would raise it by one |
|---|---|---|---|
| Completeness | 3 | All six documents are present, but some required elements are thin or missing. The PLAN_SPEC row for 15 requires a "capacity and cost model", and 15 sends it elsewhere: `15-deployment-and-operations.md:26` "Sizing numbers, thresholds and cost \| `28-capacity-and-cost-model.md`". 16 depends on a version row that 19 does not have: `16-test-strategy.md:359` "FsCheck ... unverified until document 19 pins the version". 19 has no FsCheck row (grep finds none). 17 has no Integrations capability in phase 4 or phase 5, and 20 still places REQ-INT-016 in those phases: `20-traceability-matrix.md` row REQ-INT-016 "4, 5 \| SL-INT-411, SL-INT-600". 20 fills 333 test cells with derived identifiers that no test specifies yet: `20-traceability-matrix.md:30` "the requirement's acceptance test takes the identifier **`TC-<AREA>-(950 + requirement number)`**". | Add a capacity and cost summary to 15, or a spec-approved pointer to it. Add an FsCheck row to 19. Add the proposed CAP-INT-02 (phase 4) and CAP-INT-03 (phase 5) to 17. |
| Consistency | 2 | The documents contradict each other and themselves in many places. (1) 17 against itself: `17-roadmap.md:25` gives phase 1 "16" workflows, which only works if WF-INF-01 and WF-INF-03 count there, but `17-roadmap.md:219` says those two are "in phase 6" while `:30` gives phase 6 "0 new". (2) `17-roadmap.md:223` "about 38 capabilities": phases 1 and 2 hold 38, plus 4 from phase 3, which makes 42. (3) `17-roadmap.md:45` "\| 1 \| INT \| 17 \| REQ-INT-001 to 017", but 20 puts REQ-INT-014 and 015 in phase 3 and REQ-INT-016 in phases 4 and 5. (4) 18 moves School's contract a phase early: `18-risk-register.md:83` "School directory gRPC contract ... published in the first capability of Phase 1", against `17-roadmap.md:280` "week 1 of phase 2". (5) `16-test-strategy.md:622` "Plus 4 \| Plus 3" for phases 4 and 5, where 17 lists 3 and 4 services. (6) `16-test-strategy.md:628` adds N-06 in phase 4, where `18-risk-register.md:100` says "N-06 ... run nightly from Phase 3". (7) `15-deployment-and-operations.md:692` "Eight procedure runbooks", where the table lists 11, so the total is 63, not 60. (8) `19-dependency-and-license-inventory.md:314` "11 tests, passing" against `:382` "12 tests". (9) `16-test-strategy.md:19` "PostgreSQL 16 ... Valkey 8" against 19's rows for 18.6 and 9.1.2. (10) `18-risk-register.md:175` leaves RISK-03 out of the list of 12-score risks outside the top ten. | Reconcile the phase counts in 17 with section 4 and with 20's phase column. Align the School contract timing in 18 with 17. Align the phase service counts and N-06 timing in 16 with 17 and 18. Correct the runbook count in 15, the test count in 19 and the version pins in 16. |
| Feasibility | 2 | The plan's own formula contradicts the phase 1 range. `34-work-breakdown.md:36` "\| 1 \| 19 \| 179 \| 423 \| 8 to 10 weeks", and `:44` converts slice-days to calendar time as "slice-days divided by the number of engineers ... multiplied by about 1.3". With the brief's team of about 5.5 to 8.5 engineers, that gives 13 to 20 weeks for phase 1. `17-roadmap.md:25` still says "8 to 10 weeks", and the only proof it offers is future: `17-roadmap.md:327` "Re-estimated at the end of each phase". The nightly load gates depend on a tool the policy blocks: `19-dependency-and-license-inventory.md:224` "k6 ... not-allowed until a Section 6.4 row and ADR exist". A 24-hour soak also cannot run "nightly on the load tier" (`16-test-strategy.md:499`) on the same tier as ten nightly gate scenarios and chaos. | Re-estimate the 17 phase ranges from the 34 totals and the Section 29 team, and show the arithmetic. Land the k6 Section 6.4 ADR. Give the load tier a schedule with capacity (soak weekly, or its own environment). |
| Risk honesty | 3 | Each register row has likelihood, impact, owner and a signal. But a fired signal was not re-scored: `18-risk-register.md:79` RISK-03 signal "Sum of slice ranges for a phase exceeds the top of its Section 28 range" has fired, yet the risk stays "\| 4 \| 3 \| 12 \| ... \| Open". The register has no risk for AI output safety or hardware, the k6 licence blocker, the pending product decisions (absence-alert 30 min vs 30 s, Tier 1 API), or the roughly 40 appendix defects logged in brief-findings. Nine brief-findings items touch this group; for example 17 still promises demos that brief-findings says Appendix O minutes 1, 5, 10, 12 and 14 cannot meet. RISK-34 claims drills that 17 schedules in phase 6: `18-risk-register.md:130` "Quarterly drills from Phase 1". | Re-score RISK-03 to 5 against the 34 totals. Add risks for Ai, k6, the pending product decisions and the brief v9.1 corrections, each with an owner. Align RISK-34 with the phase 6 drill capability. |
| Testability | 3 | 20 gives every requirement a test id, but 39% (333 of 860) are derived ids with no written test. Several verification claims rest on checks that do not exist. `18-risk-register.md:226` "Document 20 carries each `TC-` identifier named in part 3; `/lint-plan` refuses one absent there", yet TC-TST-220, TC-PLAT-012, TC-PLAT-015, TC-SEC-056, TC-SEC-391, TC-PRV-051 and TC-L10N-801 are absent from 20. `/lint-plan` only runs kit-lint, which has no such rule (R19 does not cover TC ids). `16-test-strategy.md:769` makes the same false claim. `15-deployment-and-operations.md:841` says 16 "registers" TC-INF-101 and above, but TC-INF-105, 106 and 110 appear in neither 16 nor 20. The TC-PLAT-002..005 and TC-DOC-002 collisions from brief-findings remain. | Add the missing TC ids to 20, or register them in 16 section 17. Either build the claimed checks (a kit-lint rule for TC ids in 20 and a risk-table check) or restate the "How this document is verified" rows as review-only. Resolve the two id collisions. |
| Distinctiveness | 3 | These are support documents, so the axis is scored on how well they back the signature features. 16 claims more than the plan delivers: `16-test-strategy.md:659` "Every Appendix W signature feature has its Appendix O row". Brief-findings records that 23 of 44 signature features have no Appendix O minute. 15 builds feature 39 in depth (`15-deployment-and-operations.md:698` "run on each tenant's own school calendar and time zone, before first period"), but brief-findings proposes demoting it because it cannot be shown on demo data. RISK-31 is sharp: `18-risk-register.md:122` "the sales claim is 'bilingual data, not a translated interface'". | Correct 16:659 to the real coverage and name the reserve-bank proofs. Settle feature 39's status in 15 and 17. Align the 17 phase demo claims with the Appendix O minutes. |
| Portability | 4 | Platform cases are named with runners: `16-test-strategy.md:701` "TC-PLAT-015 ... PgBouncer in transaction mode \| integration \| Linux". The device pass covers a device without Google services (`16-test-strategy.md:451`), and `15-deployment-and-operations.md:299` runs dev-smoke on "`ubuntu-latest` and `windows-latest`". Gaps: `15-deployment-and-operations.md:119` claims "macOS 14" for developers, but no runner proves it. 20 gives the runner only as "per Appendix X" for the platform requirements REQ-PLAT-015..023, where it matters most. The appliance drill is Hyper-V only in `15-deployment-and-operations.md:517`, while 34 SL-INF-614 drills VMware too. | Name the runner in 20's Platform column. Either prove the macOS developer setup on a runner or drop the claim. Add VMware to the 15 drill schedule. |

**Verdict:** Blocked (blocking axes: Completeness, Consistency, Feasibility, Risk honesty, Testability, Distinctiveness)

| Gap | Axis | Effort to close | Blocking |
|---|---|---|---|
| The phase 1 range in 17 (8 to 10 weeks) conflicts with 34's 423 slice-days: 13 to 20 weeks at the brief's team size. The MVP line (30 to 36 weeks, "about 38" capabilities that are really 42) needs recomputing too | Feasibility | 1 day | yes |
| 17 is inconsistent with itself and with 20: phase 1 and phase 6 workflow counts, INT phase placement, the proposed CAP-INT-02 and CAP-INT-03 are absent, the OneRoster phase | Consistency | 0.5 day | yes |
| "How this document is verified" rows in 15, 16, 17 and 18 cite `/lint-plan` checks that do not exist (TC ids in 20, risk-table arithmetic, quoted-table diffs, workflow-to-capability) | Testability | 2 days to build kit-lint rules, or 2 hours to restate them as review | yes |
| TC ids cited in 15 and 18 are missing from 20 and 16 (TC-TST-220, TC-PLAT-012/015, TC-SEC-056/391, TC-PRV-051, TC-L10N-801, TC-INF-105/106/110); the TC-PLAT-002..005 and TC-DOC-002 collisions remain | Testability | 4 hours | yes |
| The risk register is stale: RISK-03's signal has fired but it was not re-scored; RISK-07 has the wrong phase; RISK-34 has the wrong drill timing; no risks for Ai, k6, the pending product decisions or the appendix defect backlog | Risk honesty | 4 hours | yes |
| k6 is AGPL and not allowed in 19, yet every load gate in 15, 16 and 17 depends on it; the load tier is also over-scheduled (nightly 24 h soak plus scenarios plus chaos) | Feasibility | 1 day (ADR plus schedule) | yes |
| Cross-document drift: service counts per phase and N-06 timing (16 vs 17 and 18), PostgreSQL and Valkey versions (16 vs 19), the procedure-runbook count (15), the scanner test count (19) | Consistency | 3 hours | yes |
| 16:659 claims every signature feature has an Appendix O row, which contradicts the 23-of-44 finding; feature 39's status is unsettled | Distinctiveness | 3 hours | yes |
| 15 has no capacity and cost model section, which the PLAN_SPEC row requires; 19 has no FsCheck row, though 16 depends on it | Completeness | 3 hours | yes |
| 20's Platform column says "per Appendix X" instead of naming a runner; the macOS developer claim is unproven; the appliance drill omits VMware | Portability | 2 hours | no |

#### 3.6 Group F: Conventions to work breakdown (22 to 29, 31 to 34)

| Axis | Score | Evidence | What would raise it by one |
|---|---|---|---|
| Completeness | 3 | Most PLAN_SPEC rows are present and deep (22 §1 to §12, 23 §1 to §8, 24 §1 to §10). Some are thin or missing. `31-business-rules-and-workflows.md:149` "Transition tests are the rows of the workflow's test table in Appendix R", so PLAN_SPEC's "transition tests per workflow" are referenced, not listed. `29-adr-index.md:5` "Every record from 0001 to 0018 is **Proposed**": none of the ADRs Group F promises (22 OP1, 23 OP2, 24 OP4, 25 OP1, 32 OP1/3/5) is indexed or listed in §3. Documents 29 and 33 have no Decisions, Open points or Review record sections. `26-migration-and-onboarding-toolkit.md:558` "`06-services/documents.md`, not yet written" is stale: the sheet exists. | List the transition tests per workflow in 31 §3. Add the Group F ADRs (proposed) to 29 §1/§3. Add Open points and Review record to 29 and 33. Close 26 OP4 now that documents.md exists. |
| Consistency | 2 | kit-lint R19 is clean, but these contradictions are unresolved. `23-integrations-and-public-api.md:469` "OneRoster 1.2 \| 4" vs `34-work-breakdown.md:1353` "built here in phase 3, one phase earlier than the standards table". `23-integrations-and-public-api.md:39` puts webhooks in the "Tier 2 public set", but `34-work-breakdown.md:1332` "Phase 1 builds the keys and webhooks". `31-business-rules-and-workflows.md:202` WF-INF-01 is "\| 1 \|" (phase 1), but 33:202 and 34 part D put it in phase 6. `27-compliance-and-legal.md:422` "Residency holds \| `TC-PLT-001`", and 32:43 uses TC-PLT-001 for configurability. `25-ai-and-assist-ladder.md:140` "ML.NET models loaded by Reporting, Assessment and Hr … emit `ai.usage.recorded.v1`" breaks document 11's own-exchange rule. `34-work-breakdown.md:1333` "the merger must give them slices" is a leftover assembly instruction. | Align the OneRoster phase and the webhook tier in 23 §1/§6 with 34 (or the reverse). Set WF-INF-01/03 to phase 6 in 31. Give TC-PLT-001 one meaning across 26, 27 and 32. Apply the plan-level fixes to 25 (usage.recorded, embedding_chunk, REST not gRPC). Delete the stale merger notes in 34. |
| Feasibility | 3 | `34-work-breakdown.md:36` phase 1 is "179 \| 423 \| 8 to 10 weeks". `34-work-breakdown.md:44` "divided by the number of engineers … multiplied by about 1.3". Master brief:1214 gives "two to four backend engineers, one to two web engineers, one mobile engineer, one quality engineer". At 8 engineers, phase 1 is about 14 weeks, more than 25% over the range, and 34 defers it: "re-estimated at the end of phase 0". 28 prices are "illustrative until the provider quote" (28:474), and 28 OP4 says no N scenario runs on a single server. | Do the slice-days ÷ team × 1.3 check per phase in 34 §2 against the 17 ranges now. Re-cut or re-range phase 1. Record the result in 17 and 00. |
| Risk honesty | 3 | Open points name an owner, a default and an impact, e.g. `28-capacity-and-cost-model.md:501` "storage is 34% of cost; erasure coding would cut the storage line by about half". None gives a likelihood. 29 and 33 have no open points, although 33:108 names "the Apple build capacity line … open question 14" as pending. `34-work-breakdown.md:1417` "Re-estimated at the end of phase 0 … the slices are not shortened to fit" never states the phase 1 overrun as a risk. Brief-findings lists about 16 defects that touch these documents (JOD rounding, يوسف recall, autonomy column, 402 vs fallback, the absence-alert timing and more), and most sit only in open points, not in 18. | Add likelihood to every open point and cross-reference RISK ids in 18 for the phase 1 overrun, OneRoster/Tier 1 pending, Apple capacity and the unpinned Arabic model. Give 29 and 33 open-point tables. |
| Testability | 3 | 22–24 map claims to ids well, e.g. `22-api-conventions-and-error-catalog.md:775` "`TC-API-052`: 501 items is 400". Others have none. `26-migration-and-onboarding-toolkit.md:586` "Table-driven tests per §3.4 to §3.6" and 26:587 "Golden-output test per adapter fixture". `25-ai-and-assist-ladder.md:528` "Quality in both languages \| §6.2 thresholds over the §6.1 golden sets". `28-capacity-and-cost-model.md:516` "a reviewer re-running any row gets the same figure". Ids collide: `32-product-differentiation-and-demo.md:267` "TC-DOC-002 … while Appendix W assigns TC-DOC-002 to feature 36". | Give TC ids to the 26 validation and adapter tests, the 25 harness thresholds and the 28 arithmetic check. Resolve the TC-PLT-001 to 005 and TC-DOC-002 collisions by ADR. |
| Distinctiveness | 3 | 32 §1 gives all 44 features a moment, a rung and a competitor cell. But `32-product-differentiation-and-demo.md:84` "Twenty-one features have a minute in Appendix O; twenty-three do not". Eighteen fall back to reserve steps that are not release-gated (32:265 "Reserve steps rot between releases"). Feature 39 has no proof (32:198 "Not showable on demo data"). `32-product-differentiation-and-demo.md:153` "Minutes 1, 5 and 10 cannot run as scripted" at the phase 2 demo. | Ratify the reserve bank into Appendix O by ADR so every R-step is gated. Decide demote or IT-lead evidence for feature 39. Amend the 17 phase-demo claims to the 32 §3.1 substitutions. |
| Portability | 4 | 33 names a runner and a TC-PLAT id per edge case, e.g. `33-platform-support-and-dev-environments.md:229` "the money rule tests run under `ar-SA`, `en-US` and `de-DE` … on both runners". 24:820 pins culture under three cultures. Gaps: `33-platform-support-and-dev-environments.md:253` "macOS is verified by the mobile engineer's machine at each phase demo" has no runner. The iOS lane (33:108) depends on open question 14 being answered. Huawei push stays Tier 2 (29:148). | Add a macOS `verify-setup` job, or a recorded checklist with an id. Attach a default and a fallback to the iOS lane in case OQ14 slips. Name the no-Google push fallback test id in 33 §7. |

**Verdict:** Blocked (blocking axes: Completeness, Consistency, Feasibility, Risk honesty, Testability, Distinctiveness)

| Gap | Axis | Effort to close | Blocking |
|---|---|---|---|
| Phase 1 is 423 slice-days against 8–10 weeks. At the stated team (5–8 engineers × 1.3) that is 14 to 18 weeks, and 34 defers the check to phase 0 | Feasibility | 1 day (re-range in 17, 34 §2 table, 00 update) | yes |
| Cross-document contradictions: OneRoster phase 3 vs 4, webhooks Tier 2 vs built in phase 1, WF-INF-01 phase 1 vs 6, stale merger notes in 34:1333 | Consistency | 0.5 day | yes |
| Test-id collisions: TC-PLT-001 means residency (27), configurability (32) and provisioning (26); TC-DOC-002 means QR verification and yearbook | Consistency, Testability | 1 day plus an ADR amending Appendix W | yes |
| 25 still carries plan-level defects its own sheet rejected: ai.usage.recorded.v1 published by Reporting/Assessment/Hr, gRPC to Ai, `embedding_chunk` vs `embeddings` in 10/21 | Consistency | 0.5 day | yes |
| Claims with no TC id in 25 (§6.2 thresholds), 26 (validation, adapters) and 28 (arithmetic, calendar scaling) | Testability | 1 day | yes |
| 29 indexes only 18 Proposed ADRs and omits every ADR that Group F open points say is needed. 29 and 33 lack Open points and Review record sections | Completeness, Risk honesty | 0.5 day | yes |
| Open points carry no likelihood and do not link to 18. Pending product-owner decisions (Tier 1 API/OneRoster/iCal, absence-alert 30 min vs 30 s, countries/Apple/certifications) have no RISK ids | Risk honesty | 0.5 day | yes |
| 23 of 44 signature features rely on reserve steps not in the release gate. Feature 39 has no proof. Phase 2 demo minutes 1, 5 and 10 cannot run as scripted | Distinctiveness | 1 day plus an ADR on Appendix O | yes |
| 31 does not enumerate transition tests per workflow, which PLAN_SPEC requires | Completeness, Testability | 1 day (generator change) | yes |
| macOS developer setup has no runner. The iOS pipeline depends on unanswered OQ14 | Portability | 2 hours | no |

### 4. Findings re-checked by hand

| Finding | Checked against | Result |
|---|---|---|
| Document 02 says seven of ten competitors publish an API | `02-competitive-gap-analysis.md` lines 69, 96, 119 and its matrix | Confirmed: the text says seven, the matrix supports six |
| The Gateway cannot read the revoked-user mark, so a signed-out leaver keeps a working token | `06-services/identity.md` open point 13 and `21-performance-engineering.md` Section 2.3 | Confirmed: Identity writes `state:revoked:`, document 21 grants `svc_gateway` no read on it |
| Every load gate depends on k6, which the dependency inventory does not allow | `19-dependency-and-license-inventory.md` line 224 | Confirmed: k6 2.3.0, AGPL-3.0, "not-allowed until a Section 6.4 row and ADR exist" |
| Phase 1 is 423 slice-days against a roadmap range of 8 to 10 weeks | Master brief Section 29 team shape (two to four backend, one to two web, one mobile, one quality engineer) and document 34 Section 2 | Confirmed: 423 × 1.3 / (5 to 8 builders × 5 days) is 14 to 22 weeks |
| Documents 10 and 21 name the AI index `embeddings`; document 25 names `embedding_chunk` | `10-data-architecture.md` line 266 | Confirmed |

### 5. What blocks approval, across groups

The group gap lists overlap. Consolidated, the work before approval is eight themes, in the order they should be done because later ones depend on earlier ones.

| # | Theme | Groups | What closes it | Effort |
|---|---|---|---|---|
| 1 | **Product-owner decisions** | A, B, D, E, F | Absence-alert timing; public API, OneRoster and iCal in Tier 1; target countries, Apple build capacity and certifications. Each is recorded in `OPEN_QUESTIONS.md`, document 01 and a RISK row in document 18 until answered | 1 hour of the owner's time; 3 hours to record |
| 2 | **Schedule matches the work breakdown** | E, F | Re-range every phase in document 17 from document 34's slice-days at the Section 29 team shape, recompute the MVP line, and state the team size each range assumes | 1 day |
| 3 | **Brief corrections (v9.1)** | all | ADR-0019 applies the logged appendix defects (`tools/plan-build/parts/brief-findings.md`): missing events, permissions and error codes, the k6 Section 6.4 row, the proposed CAP-INT-02 and CAP-INT-03 in the roadmap, the conflicting values (cooling-off, invitation expiry, dedupe window), JOD decimals, Appendix W's autonomy column | 3 days |
| 4 | **One meaning per fact across plan documents** | A to F | Fix the named contradictions: the synchronous-call graph in 04, 05 and 07; `embedding_chunk` in 10 and 21; the Gateway's Redis reads in 21; the permission-refresh owner in 08, 11 and 12; OneRoster's and WF-INF-01's phase; webhooks' tier; Finance's numeric precision; the API count in 02 and 32 | 3 days |
| 5 | **One test-identifier registry** | A, B, D, E, F | Make document 16 the registry of every `TC-` identifier, resolve the collisions (TC-PLT-001 to 005, TC-DOC-002), register the locally minted ones, and add a kit-lint rule that every cited `TC-` identifier is registered exactly once | 2 to 3 days |
| 6 | **Verification claims that run** | A, D, E, F | Every "How this document is verified" row either names a check that exists or is restated as a review step; the missing kit-lint rules worth having are built (open questions compared with document 01, risk-table arithmetic) | 2 days |
| 7 | **Risk honesty everywhere** | A to F | Every Open point carries likelihood and a RISK identifier; documents without an Open points or risk section get one (03, 08, 09, 10, 11, 12, 14, 21, 29, 33); document 18 re-scored (RISK-03, RISK-07, RISK-34) and extended (AI, k6, pending decisions, the brief backlog) | 1.5 days |
| 8 | **Every signature feature proven on the stage** | A, C, E, F | The 23 of 44 features without a release-gated demo step get one in Appendix O and document 32; feature 39 gets its proof; the phase 2 demo minutes that cannot run as scripted are re-scripted | 1.5 days |

**Total: about 14 to 15 working days of plan work**, plus the product owner's decisions. Themes 2, 4, 5 and 6 lift Consistency, Feasibility and Testability, the three lowest axes.

### 6. Re-score

Each group is re-scored with `/score-plan` after the themes that touch it are closed, and the new scores replace Section 2 with the old ones kept in the review record. The plan is approved when every group is at 4 or better on every axis, and document 00 is then written and scored last.

## Decisions in force

| Decision | Record |
|---|---|
| Approve only at 4 or better on every axis | `PLAN_SPEC.md`, "The scorecard" |
| Scores need quoted evidence; a score without evidence is not a score | `.claude/commands/score-plan.md` |
| Brief defects are corrected together under one ADR and a version bump | CLAUDE.md working rules; ADR-0019 (to be written) |

## Dependencies on other documents

| Document | What this one takes |
|---|---|
| `PLAN_SPEC.md` | The rubric and the approval rule |
| Every plan document in Groups A to F | The evidence |
| `34-work-breakdown.md` | The slice-days behind the feasibility scores |
| `20-traceability-matrix.md` | The test coverage behind the testability scores |

## Open points

| Point | Default | Owner |
|---|---|---|
| The three product-owner decisions in Section 5 theme 1 | The plan follows the requirement as written until decided | Product owner |
| Whether to build or restate the missing kit-lint checks (theme 6) | Build the two with the most leverage, restate the rest as review steps | Architect |

## Review record

| Date | Reviewer | Result |
|---|---|---|
| 2026-09-22 | Six independent adversarial reviewers, one per group; four findings re-checked by hand | Groups A to F blocked; lowest axes Consistency (2) and Feasibility (2); remediation of about 14 to 15 days in eight themes |

## How this document is verified

| Claim | Proof |
|---|---|
| Every score has evidence | Each row of Section 3 quotes a document with file and line; a row without one is invalid |
| The summary table matches the group scores | Section 2 is computed from Section 3 by the assembly script, not typed |
| The most consequential findings are real | Section 4 records the hand check of each |
| The verdict follows the rule | A group with any axis below 4 is shown blocked; the script applies the rule |
