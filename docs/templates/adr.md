# ADR-`<NNNN>`: `<Decision, stated as a claim>`

> Copy to `docs/project/DECISIONS/<NNNN>-<kebab-title>.md`. The title names the decision, not the topic.

- **Status:** `<Proposed | Accepted | Superseded by ADR-NNNN>`
- **Date:** `<YYYY-MM-DD>`
- **Deciders:** `<names or roles>`
- **Requirement IDs:** `<REQ-AREA-NNN, ...>`

## Context

What forces are at play, what the brief or the reference architecture says, and what changed to make this a decision rather than a default.

## Decision

What was decided, in a form someone could act on without asking a question.

## Alternatives considered

| Option | What it would cost | Why it lost |
|---|---|---|
| `<option>` | `<cost>` | `<reason>` |
| `<option>` | `<cost>` | `<reason>` |

At least two. If there was genuinely one option, say what forced it.

## Consequences

| Consequence | Easier or harder | Who feels it |
|---|---|---|

**Revisit when:** `<a concrete trigger, such as a tenant exceeding 20,000 students, not "later">`

## Downstream changes made

| Document | What changed |
|---|---|

| Open question | Closed or opened |
|---|---|