# Wow moment: `<feature>`

> Copy into `docs/brief/02-appendices/appendix-p-differentiation.md`. One feature, one moment, sixty seconds. If it cannot be shown in sixty seconds, it is not yet a differentiator, and this page says so.

**Feature** `<name>` · **Owning service** `<Service>` · **Tier** `<1 | 2 | 3>` · **Requirement IDs** `<REQ-AREA-NNN>`

## Competitors

| Product | What it does today | How well | How I verified |
|---|---|---|---|
| `<product>` | `<actual behavior, not marketing>` | `<good | adequate | absent>` | `<source>` |

Name at least one thing a competitor does better. A page that claims total superiority is not believed.

## Our edge

`<One sentence naming the difference, and the mechanism that produces it. "Better user experience" is not an edge.>`

## Persona moment

| Question | Answer |
|---|---|
| Who | `<named person, role>` |
| When in the school year | `<date and why that date matters>` |
| What they were doing | `<task>` |
| What they felt before | `<emotion>` |
| What they feel after | `<emotion>` |

## Sixty-second proof

| Second | Screen | Action | What the viewer sees |
|---|---|---|---|
| 0-10 | `<screen>` | `<action>` | `<visible change>` |
| 10-30 | `<screen>` | `<action>` | `<visible change>` |
| 30-60 | `<screen>` | `<action>` | `<visible change>` |

**Timed at** `<seconds>`. Over sixty seconds means the feature is not yet demonstrable, and the verdict below says so.

## Demo data required

| Data | Present in the seeded tenant | Where it is defined |
|---|---|---|

## Defensibility

| Question | Answer |
|---|---|
| What a competitor must build to erase this | `<what>` |
| How long that would take them | `<estimate>` |
| What makes it hard | `<the mechanism, not the effort>` |

## Verdict

**`<Show it | Fix the demo | Refuse>`** — `<reason>`