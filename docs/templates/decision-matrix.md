# Decision matrix: `<the choice being made>`

> Use when comparing options on more than two axes. Weights are set **before** the options are scored, and they are not adjusted afterwards to produce the preferred answer.

**Decision** `<what is being chosen>` · **Decider** `<who>` · **Date** `<YYYY-MM-DD>` · **Becomes** `<ADR-NNNN>`

## Criteria and weights

| Criterion | Weight | Why it matters here | Who set it |
|---|---|---|---|
| `<criterion>` | `<1-5>` | `<reason>` | `<who>` |

Weights are fixed at `<date>`, before any option was scored.

## Options

| Option | Summary | Who proposed it |
|---|---|---|

## Scores

| Criterion | Weight | `<Option A>` | `<Option B>` | `<Option C>` |
|---|---|---|---|---|
| `<criterion>` | `<n>` | `<1-5>` | `<1-5>` | `<1-5>` |
| **Weighted total** | | `<sum>` | `<sum>` | `<sum>` |

## Evidence

| Option | Criterion | Evidence for the score |
|---|---|---|

A score with no evidence is an opinion wearing a number.

## Disqualifiers

| Option | Disqualified by | Rule |
|---|---|---|
| `<option>` | `<license, privacy, or safety rule>` | `<the rule it breaks>` |

A disqualified option scores nothing. A licence or child-safety failure is not traded against convenience.

## Outcome

**Chosen:** `<option>`

**Why, in one paragraph, beyond the arithmetic:** `<reason>`

**What would change this:** `<the fact that would flip the decision>`