# Release notes `<version>`

> Written for a head teacher, not an engineer. Both languages are complete, and neither is a literal translation of the other. Nothing that changed for the user is omitted.

**Version** `<version>` · **Date** `<YYYY-MM-DD>` · **Applies to** `<web, mobile, both>`

---

## English

### What is new

- **`<Feature name>`** — `<what it lets the school do, in one sentence>`

### What is better

- **`<Area>`** — `<the improvement, and what the user will notice>`

### What is fixed

- `<the problem as the school experienced it, not the code that was wrong>`

### What you need to do

| Action | Who | By when |
|---|---|---|

### Known issues

| Issue | Who it affects | Workaround | Expected fix |
|---|---|---|---|

---

## العربية

### الجديد

- **`<اسم الميزة>`** — `<ما الذي يتيحه للمدرسة، في جملة واحدة>`

### تحسينات

- **`<المجال>`** — `<التحسين وما سيلاحظه المستخدم>`

### إصلاحات

- `<المشكلة كما واجهتها المدرسة>`

### ما المطلوب منك

| الإجراء | المسؤول | الموعد |
|---|---|---|

### مشكلات معروفة

| المشكلة | من تؤثر عليه | حل مؤقت | موعد الإصلاح المتوقع |
|---|---|---|---|

---

## Internal notes (not published)

| Item | Detail |
|---|---|
| Migrations | `<expand, migrate, contract steps>` |
| Rollback | `<procedure, and when it was last rehearsed>` |
| Restore test | `<tenant, size, duration, result>` |
| Requirement IDs completed | `<REQ-AREA-NNN, ...>` |